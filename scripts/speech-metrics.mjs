export function normalizePersianTranscript(value){return value.normalize('NFC').replace(/[يى]/g,'ی').replace(/ك/g,'ک').replace(/[\u064B-\u065F\u0670]/g,'').replace(/[۰-۹٠-٩]/g,c=>String('۰۱۲۳۴۵۶۷۸۹'.includes(c)?'۰۱۲۳۴۵۶۷۸۹'.indexOf(c):'٠١٢٣٤٥٦٧٨٩'.indexOf(c))).replace(/\u200c/g,' ').replace(/[^\p{L}\p{N}\s]/gu,' ').replace(/\s+/g,' ').trim();}
export function editDistance(a,b){let previous=Array.from({length:b.length+1},(_,i)=>i);for(let i=0;i<a.length;i++){const row=[i+1];for(let j=0;j<b.length;j++)row.push(Math.min(row[j]+1,previous[j+1]+1,previous[j]+(a[i]===b[j]?0:1)));previous=row;}return previous[b.length];}
export function speechErrors(reference,hypothesis){const ref=normalizePersianTranscript(reference),hyp=normalizePersianTranscript(hypothesis);const words=ref.split(' ').filter(Boolean),chars=[...ref.replace(/ /g,'')];return{words:words.length,characters:chars.length,wordErrors:editDistance(words,hyp.split(' ').filter(Boolean)),characterErrors:editDistance(chars,[...hyp.replace(/ /g,'')])};}
