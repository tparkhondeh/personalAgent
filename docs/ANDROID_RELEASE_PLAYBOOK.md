# راهنمای دائمی ساخت و تحویل Android

## قاعده دائمی ظاهر و شواهد — کاندید ۲۹

- پیش‌فرض روشن مستقل از تم سیستم است؛ تیره فقط با انتخاب صریح ذخیره‌شده. `AppearanceController` پیش از اولین بارگذاری، مقدار معتبر را به سند می‌دهد و همان مقدار برای صفحه بازیابی، رابط محلی و Loading استفاده می‌شود.
- پیروی خودکار CSS از OS یا Algorithmic Darkening نباید دوباره فعال شود. هر پالت از globals.css تولید شود، نه رنگ‌گذاری جداگانه APK.
- آزمون اجباری: سیستم تیره + نصب تازه = روشن؛ انتخاب تیره + Cold Launch = تیره؛ تغییر سیستم به روشن نباید انتخاب تیره را پاک کند؛ بازگشت صریح به روشن باید محفوظ بماند. صفحه بازیابی و رابط محلی هر دو کنترل شوند.
- رنگ WebView به‌تنهایی کافی نیست: پرچم واقعی کنتراست نوار وضعیت/پیمایش نیز با dumpsys بررسی و تصاویر آن بازبینی شوند. SystemBars در Capacitor سبک resolved قبلی را پس از Configuration برمی‌گرداند؛ MainActivity بعد از lifecycle رنگ و کنتراست را از انتخاب صریح بازیابی می‌کند. آزمون android-bar-appearance باید نوار تیره با آیکون تیره را رد کند. کاندید ۲۹ به همین دلیل با وجود قبولی آزمون قدیمی منتشر نشد.
- پیش از Dispatch ساخت پرهزینه، نسخه محلی رابط داخل APK و صفحه بازیابی را در هر دو تم بازبینی کنید؛ از خواندن خط‌های عظیم generated HTML در خروجی ابزار خودداری کنید. اشتراک tokenها کافی نیست: computed style دکمه‌ها/حاشیه‌ها و کنتراست متن‌های صفحه بازیابی (حداقل ۴٫۵) نیز باید کنترل شوند. موفقیت کاندید ۳۲ ضعف کنتراست badge/note تیره را پوشش نمی‌داد؛ gate مربوط اضافه شد.
- `WebViewCompat` وابستگی مستقیم app است. تزریق سند پیش از `Bridge.load()` و پیام فقط از main frame مبدأ دقیق معتبر مجاز است. مقدار ذخیره‌شده فقط enum است؛ داده برنامه یا حساب وارد این پل نشود.
- Workflow پیش‌فرض قبل از انتشار برای بازبینی شواهد توقف می‌کند. فایل دانلودشده باید همان Artifact آزموده‌شده و دارای SHA یکسان باشد؛ شکست 403 انتشار قبلی را با دست‌کاری مجوزهای حساب حل نکن.

این سند حاصل خطاهایی است که هنگام ساخت نسخه Android پروژه «همراه» رخ داد. هدف آن این است که صفحه سفید، لینک منقضی، اشتباه‌گرفتن APKها و اعلام موفقیت زودهنگام دوباره تکرار نشوند.

## نسخه آزمایشی فعلی — ۲۸

- نام `همراه پایدار 28`؛ Package `ir.wealthos.personalagent.stable28`؛ نسخه `1.0.28`.
- Commit داخل APK و Staging: `f363336070cf1d84968b95013c07cbb52e5bbf83`.
- SHA-256: `eee548c9a9e9cc0476d7ae84460fa45e45dbef3d226c74ab0f0ce96b325e04a8`.
- [دانلود دائمی](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-28/Hamrah-stable-28.apk)؛ [آزمون سه Android](https://github.com/tparkhondeh/personalAgent/actions/runs/34027180347)؛ [شواهد و محدودیت‌ها](COMPACT_APPROVAL_2026-09-06.md).
- ۱۱ تست بومی، چهار کنترل تطبیق/کیبورد و ۲۱ بررسی UI سیستم در هر Android موفق؛ ۱۲ تصویر تازه بصری بدون پوشش خطا تأیید شدند. ۲۰۷ تست محلی، Type Check، Lint، Build و ۳۵ کنترل HTTP Staging موفق‌اند.
- ساخت APK یک بار در `34023397715` انجام شد؛ بازآزمایی با Commit ابزار `8b1270c1c79647b9a02bb2451a614fd96ad59329` همان فایل را بدون بازسازی آزمود. Build source و QA source را جدا گزارش کنید.
- انتشار حساب CI با 403 رد شد، اما هر سه emulator موفق بودند. پس از بررسی شواهد، همان فایل با اتصال مجاز مالک به GitHub، ابتدا Draft و بعد از تطبیق digest همه فایل‌ها منتشر شد؛ دریافت دوباره URL عمومی SHA برابر داد. سطح دسترسی یا حفاظت مخزن تغییر نکرد. شکست CI publication را با شکست آزمون برنامه یا موفقیت کلی Workflow یکی نگیرید.
- آنلاین Staging با مرورگر/HTTP بررسی شد؛ شبکه runner آن را نمی‌دید و recovery آزموده شد. پذیرش اتصال روی گوشی، Push واقعی و سرویس‌های اختیاری همچنان جدا باقی‌اند. نسخه قدیمی گوشی و اطلاعات محلی‌اش را حذف نکنید.

## سابقه نسخه آزمایشی — ۲۷

- نام `همراه پایدار 27`؛ Package `ir.wealthos.personalagent.stable27`؛ نسخه `1.0.27`.
- Commit برنامه داخل APK و Staging: `f3ad613f894b0d9032a5faf915ba3ded543db9fe`.
- SHA-256: `4489b31909afe90985b2ad9435fbdefbd137ea6fb5e291fa9163fe2615d15c9f`.
- [دانلود دائمی](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-27/Hamrah-stable-27.apk)؛ [CI سه Android](https://github.com/tparkhondeh/personalAgent/actions/runs/34021252124)؛ [گزارش تحویل](RELEASE_27_HANDOFF_2026-09-06.md).
- فایل منتشرشده بازدانلود و با SHA همان Artifact آزموده‌شده برابر شد. ۱۷۵ تست وب و ۱۱ تست بومی در هر Android موفق‌اند؛ چهار گزارش تطبیق WebView در هر سیستم نیز موفق‌اند. اصلاح سبک برنامه‌ریزی و Notification/پیگیری هشدار در وب، Staging و منابع بسته اعمال شده‌اند.
- بازبینی نه تصویر، پنجره `Pixel Launcher isn't responding` را روی Android 13/14 نشان داد؛ ANR مربوط به `com.google.android.apps.nexuslauncher` پیش از نصب در لاگ آماده‌سازی ثبت شده است. تصاویر ۱۶ سالم‌اند. موفقیت خودکار جای بررسی پنجره‌های سطح سیستم را نمی‌گیرد؛ تأیید بصری کامل ۱۳/۱۴ و پذیرش گوشی هنوز معوق‌اند. این نسخه کاندید آزمایش است، نه نسخه عمومی نهایی.

### درس تازه: پنجره سیستم روی WebView

کنترل متن/DOM/رنگ WebView می‌تواند پشت پنجره ANR سیستم هم موفق شود. قبل از تحویل نهایی، تصویر و درخت UI سطح Android نیز باید نبود پنجره Crash/ANR روی برنامه را نشان دهند. ANR Launcher را با Crash همراه یکی نکنید، اما تصویر پوشیده را قبول نکنید. تصاویر/لاگ اولیه حفظ شوند؛ غیرفعال‌کردن Launcher، پنهان‌کردن لاگ یا ادعای رفع اشکال سیستم مجاز نیست. افزودن gate سطح سیستم و آزمون دوباره همان فایل با تصویر بدون پوشش، کار باقی‌مانده کنترل کیفیت است.

تکمیل زیرساخت آزمون کارت کوتاه: `android-system-ui-check.mjs` کنار هر عکس، درخت UI و وضعیت پنجره را نگه می‌دارد و با پنجره Crash/ANR یا نبود hierarchy معتبر شکست می‌خورد. فقط در آماده‌سازی پیش از نصب، پنجره دقیق Pixel Launcher می‌تواند یک بار با دکمه Wait استاندارد و ثبت تصویر اولیه منتظر بازیابی بماند؛ هیچ خطای همراه بسته/پنهان نمی‌شود. کنترل کیبورد با لمس واقعی فیلد، مشاهده وضعیت IME و بررسی جای دکمه و ورودی اجرا می‌شود؛ کوچک‌کردن viewport مرورگر جای این کنترل را نمی‌گیرد.

پس از تکرار Launcher ANR در دو بوت تازه Android 14 و خروج ۱۳۷ ابزار UI در Android 16، RAM شبیه‌ساز ۴۰۹۶ MB تعیین شد. آماده‌سازی پیش از نصب، فقط برای ANR تأییدشده Launcher، حداکثر یک reboot کامل شبیه‌ساز با حفظ لاگ و تصویر هر دو بوت و تکرار warm-up دارد. اگر همراه نصب است، خطا متعلق به برنامه است، dump نامعتبر است یا بوت دوم سالم نشد، این مسیر مجاز نیست و انتشار شکست می‌خورد. این تغییر محیط CI است، نه تغییر APK و نه اثبات سلامت خودکار سیستم‌عامل.

برای اعمال ناهمگام بومی از sleep ثابت به‌عنوان اثبات اتمام استفاده نکنید: آزمون باید پایان تکمیل/حذف همان ID و لغو اعلان را مشاهده کند؛ سپس baseline عدم اثر پیش از تأیید بگیرد. مقایسه صرف تعداد، کاهشِ ناشی از حذف قبلی را هم ممکن است «ایجاد بدون تأیید» گزارش کند. مقایسه مجموعه IDها و ثبت قبل/بعد الزامی است؛ هیچ اختلافی بدون بررسی علت نادیده گرفته نشود. پس از آزمون کیبورد، لغو پیشنهاد و بازگشت/paint صفحه مقصد را نیز بررسی کنید.

گاهی UI Automator بعد از پیام نوشتن dump با خروج ۱۳۷ متوقف می‌شود. این را با Crash همراه یکی نکنید: صرفاً خواندن صفحه حداکثر سه بار، با فایل XML یکتای هر تلاش و حفظ گزارش خطا، قابل تکرار است. XML قدیمی هرگز خوانده نشود؛ اگر همان تلاش ANR/Crash را نشان داد، آن را پنهان یا Retry نکنید. خروج غیر۱۳۷، XML ناقصِ اجرای موفق و شکست مکرر هنوز مانع انتشارند. عکسِ موفق فقط پس از capture معتبر گرفته و بصری بازبینی شود. برنامه/داده/مجوزها در این Retry تغییر نمی‌کنند.

در اجرای 34026468015، خود ابزار `com.android.commands.uiautomator.Launcher` با UID پوسته 2000 هنگام `Shutting down VM`، خطای `Bad file descriptor` روی ترد UiAutomation ثبت کرد. grep سراسری FATAL آن را با خطای همراه یکی می‌گرفت. اکنون `android-logcat-check.mjs` فقط همین الگوی کامل را با تطبیق PID تمام خطوط، و فقط پس از تأیید تازه UI بدون پوشش، جداگانه به‌عنوان خرابی ابزار مشاهده ثبت می‌کند؛ لاگ کامل حفظ می‌شود. Process برنامه، PID متفاوت، الگوی ناقص، Crash ناشناخته، renderer، JavaScript و SSL ناامن همچنان Fail هستند. آزمون مستقل ضدپنهان‌شدن خطا برای این طبقه‌بندی وجود دارد.

## سابقه نسخه مرجع — ۲۶

یادداشت ۲۰۲۶-۰۹-۰۶: حذف «سبک برنامه‌ریزی» فعلاً در کد وب و لوکال اعمال شده، نه Staging. این گزینه در رابط آفلاین نسخه ۲۶ وجود ندارد؛ برای این اصلاح نیازی به تغییر فایل‌های APK نیست، اما حالت متصل فقط پس از انتشار وب روی Staging به‌روز می‌شود. نسخه نصب‌شده را خودکار به‌روز فرض نکنید.

- نام `همراه پایدار 26`؛ Package `ir.wealthos.personalagent.stable26`؛ نسخه `1.0.26`.
- Commit برنامه داخل APK `07f98c06e24b877b8934763a5f94942be76add4d`؛ آخرین Staging نیز از همین Commit است. تغییرهای منتشرنشده وب در `PROJECT_STATUS.md` ثبت می‌شوند.
- SHA-256: `b34d1bade9e13033af3af7ab1f7c355f2e9665056ce620a8d8373ea11d48b91c`.
- [دانلود دائمی](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-26/Hamrah-stable-26.apk)؛ [CI نهایی سه Android](https://github.com/tparkhondeh/personalAgent/actions/runs/34018848466)؛ [گزارش تحویل و محدودیت‌ها](SIMPLIFIED_APPROVAL_2026-09-06.md).
- فایل منتشرشده دوباره دانلود و SHA با همان Artifact تست‌شده برابر شد. تست گوشی مالک برای این نسخه معوق است؛ نسخه‌های پایین سابقه‌اند، نه لینک پیشنهادی نصب.

## یافته‌های بازبینی صوت و پالت — ۲۰۲۶-۰۹-۰۶

- پالت و فونت رابط محلی از منبع وب تولید می‌شوند؛ Android نباید با Algorithmic Darkening رنگ‌ها را دوباره تیره کند. هر دو تم در فایل APK آزموده شوند.
- Build محلی وب در `.next-build` جدا از Dev انجام شود؛ پوشه تولیدی از Lint کنار گذاشته شود، نه کد منبع.
- میکروفون: مجوز بومی و Permissions-Policy وب لازم‌اند؛ فقط مبدأ دقیق برنامه و فقط صوت مجاز است. ضبط، ارسال صوت و اجرای پیشنهاد سه مرحله جدا هستند.
- کاندید ۲۰ منتشر نشد: Android 13/16 موفق، اما در Android 14 سرویس `com.google.android.gms.persistent` هنگام به‌روزرسانی از بین رفت و سیستم، برنامه وابسته به `FontsProvider` را هم کشت. ۹ آزمون بومی و پالت/رابط قبلاً موفق بودند؛ این به معنی موفقیت کل اجرا نیست.
- وابستگی خودکار `EmojiCompatInitializer` به فونت دانلودی برای این رابط WebView ضروری نیست؛ فقط همان Initializer غیرفعال شد. Startup، فونت داخلی و ایموجی سیستم دست‌نخورده‌اند. یک تست بومی از بازگشت این وابستگی جلوگیری می‌کند. مرجع: [راهنمای رسمی Android](https://developer.android.com/reference/androidx/emoji2/text/EmojiCompatInitializer).
- خروجی ساختاریافته مدل باید همه فیلدها را required داشته باشد؛ Defaultهای سازگاری داده قدیمی فقط در Schema ذخیره‌سازی باقی بمانند. آزمون قرارداد مدل بدون مصرف API اجرا می‌شود.
- کاندید ۲۱ نیز تحویل نشد: رویداد چرخه عمر Capacitor روی سند اولیه قبل از تزریق `window.Capacitor` به `triggerEvent` دسترسی می‌گرفت. اصلاح کوچک و نسخه‌دار dependency با `pnpm patch` نگه‌داری می‌شود؛ فقط رویدادِ سندِ بدون پل/شنونده رد می‌شود، نه خطای برنامه. تست بومی هم نبود خطا قبل از آماده‌شدن و هم تحویل رویداد پس از آماده‌شدن را کنترل می‌کند؛ فیلتر Logcat حذف نشده است.

## یافته تکمیلی فونت WebView — کاندید ۲۵، ۲۰۲۶-۰۹-۰۶

حذف `EmojiCompatInitializer` فقط وابستگی AndroidX خود برنامه را حذف می‌کند؛ تست metadata، استقلال کل WebView از سرویس گوگل را اثبات نمی‌کند. WebView 109 در Android 13 مسیر مستقل `AndroidFontLookupImpl` دارد و می‌تواند فونت Noto را از GMS بخواند. [کد رسمی همان نسخه Chromium](https://chromium.googlesource.com/chromium/src/+/109.0.5414.123/content/public/android/java/src/org/chromium/content/browser/font/AndroidFontLookupImpl.java).

در تلاش اول Run 34017055661، پس از موفقیت ۱۱ تست بومی و رابط روشن/تاریک، Logcat ساعت 06:47:24 UTC نشان داد GMS به‌دلیل `Module config changed, forcing restart` خودش را متوقف کرد؛ سپس سیستم برنامه وابسته به FontsProvider را کشت. این خطای سیستم با خطای JavaScript یکی نیست و فقط با دست‌کاری ظاهر حل نمی‌شود. لاگ و شکست حفظ شدند؛ همان APK بدون تغییر و بدون حذف کنترل‌ها دوباره روی شبیه‌ساز تازه آزمایش می‌شود. اگر تکرار شود، انتشار متوقف می‌ماند. دورزدن مجوز، غیرفعال‌کردن سرویس‌های گوشی یا ادعای رفع قطعی این محدودیت مجاز نیست.

## نسخه مرجع انتشار قبلی — ۲۲

یافته نسخه ۲۶: Android 14 دوبار در میانه کار با `forcing restart due to module com.google.android.gms.rcs` و سپس مرگ وابسته به FontsProvider متوقف شد؛ ۱۱ تست بومی و رابط روشن پیش از آن موفق بودند. تکرار کور آزمون راه‌حل پایدار نیست. Workflow جداگانه `retest-existing-android.yml` همان Artifact اصلی را با کنترل SHA/Commit و موفقیت Build قبلی می‌گیرد؛ پیش از نصب، شبیه‌ساز پنج دقیقه گرم می‌شود و PID سرویس باید دو دقیقه ثابت باشد. حداکثر انتظار ده دقیقه است. سپس تمام آزمون‌های اصلی روی هر سه Android اجرا می‌شوند. هیچ بسته‌ای خاموش/تعویض، هیچ خطایی فیلتر و هیچ APKای بازسازی نمی‌شود. این صرفاً تثبیت محیط CI است، نه رفع قطعی اشکال WebView گوشی. انتشار فقط پس از موفقیت هر سه اجرا مجاز است.

- نام: `همراه پایدار 22`؛ Package: `ir.wealthos.personalagent.stable22`؛ Version: `1.0.22`.
- Commit: `158ab5422a1a38307f28fe6014ba1cc78bde9164`.
- SHA-256: `772812cebb39f0708123716f761d5248ae2e1e4c903646f08289cbba174f28fb`.
- [APK دائمی](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-22/Hamrah-stable-22.apk)؛ [CI موفق هر سه Android](https://github.com/tparkhondeh/personalAgent/actions/runs/34013442504)؛ [گزارش تحویل و محدودیت آزمون شبکه Runner](VOICE_RELEASE_HANDOFF_2026-09-06.md).
- فایل تحویلی با Artifact آزموده‌شده تطبیق دارد؛ فایل فونت، شعر، تاریخ، سه اعلان و لغو آن‌ها واقعاً آزموده شدند. نصب روی گوشی مالک برای نسخه ۲۲ **معوق** است.

پیام مهارشده `SystemBars` درباره تزریق Safe Area پیش از آماده‌شدن سند در Logcat باقی است؛ با Crash یا خطای مهارنشده اشتباه نشود و حذف/پنهان نشود. تصاویر نهایی سالم‌اند؛ حاشیه نوار سیستم و کیبورد در پذیرش گوشی نیز بررسی شوند. ۱۱ تست بومی، پالت روشن/تاریک و کانال‌های تأییدشده در هر سه نسخه موفق‌اند؛ آزمون صوت واقعی همچنان معوق است.

## سابقه تأیید گوشی — نسخه ۱۴ (نسخه فعلی نیست)

- نام روی گوشی: `همراه پایدار 14`
- Package ID: `ir.wealthos.personalagent.stable14`
- فایل: `Hamrah-stable-14.apk`
- Commit سازنده: `e26a8f044aeef874a4adda66c9adb2f3f456cb14`
- SHA-256: `b3ef3b0ce07632565a2e5db2216d0ffc7da560a8c31dc9368342c1cfb9e17e32`
- نتیجه: Android 13، 14 و 16 در شبیه‌ساز موفق؛ بازشدن برنامه روی گوشی واقعی نیز در ۲۰۲۶-۰۹-۰۵ توسط مالک تأیید شد.

## علت‌های اصلی خطا و راه‌حل دائمی

### ۱. لینک موقت داخل APK

نسخه‌های قدیمی به Pinggy یا Tunnel موقت متصل بودند. با منقضی‌شدن آدرس، برنامه خطای `ERR_NAME_NOT_RESOLVED` یا صفحه سفید نشان می‌داد.

قانون دائمی:

- هیچ APK قابل تحویل نباید به Pinggy، Cloudflare Quick Tunnel، localhost.run یا آدرس رایگان و موقت وابسته باشد.
- نسخه متصل فقط از Staging پایدار HTTPS یا Production تأییدشده استفاده می‌کند.
- رابط محلیِ داخل APK باید حتی بدون سرور قابل نمایش باشد.

### ۲. سفیدماندن WebView هنگام خطای شبکه

نمایش صرف WebView کافی نبود. برای شروع برنامه Loading بومی، Timeout بیست‌ثانیه‌ای، تشخیص محتوای خالی، مدیریت خطای بارگذاری، توقف SSL نامعتبر و صفحه بازیابی فارسی اضافه شد.

قانون دائمی:

- خطای SSL هرگز نادیده گرفته نمی‌شود؛ اتصال لغو و صفحه امن بازیابی نمایش داده می‌شود.
- خطاهای شبکه، JavaScript و Renderer ثبت می‌شوند، اما اطلاعات محرمانه به کاربر نشان داده نمی‌شود.
- صفحه بازیابی باید دکمه‌های «تلاش دوباره» و «ادامه در حالت محلی» داشته باشد.
- تغییر مقصد باید کش، Cookie، Web Storage و Service Worker قدیمی را بی‌اثر کند.

### ۳. نامطمئن‌بودن بازکردن فایل محلی

در بعضی نسخه‌های Android WebView، رفتن مستقیم از صفحه بازیابی به `https://localhost/index.html` یا دریافت `./index.html` با `fetch()` شکست خورد؛ هرچند همان مسیر در مرورگر دسکتاپ سالم بود.

راه‌حل دائمی:

- `scripts/generate-android-assets.mjs` رابط محلی، CSS و JavaScript را هنگام Build مستقیماً داخل `mobile-shell/connection-error.html` جاسازی می‌کند.
- صفحه محلی با `document.open/write/close` باز می‌شود و به Navigation یا Fetch وابسته نیست.
- `pnpm android:sync` باید همیشه Generator را پیش از Capacitor Sync اجرا کند.

### ۴. خراب‌شدن JavaScript در Generator

استفاده از متن جایگزین در `String.replace` باعث شد علامت‌های `$` و `$$` در JavaScript تغییر کنند و خطای `Identifier '$' has already been declared` ایجاد شود.

قانون دائمی:

- برای قراردادن HTML، CSS یا JavaScript دلخواه در خروجی تولیدشده، `String.replace(regex, callback)` استفاده شود؛ نه Replacement String.
- تولید Asset باید دوبارپذیر باشد و اجرای دوباره آن خروجی را خراب نکند.
- کد جاسازی‌شده پیش از Build از نظر Syntax و حضور متن واقعی فارسی آزمایش شود.

### ۵. اشتباه‌شدن نسخه قدیمی و جدید

چند APK با نام ظاهری مشابه باعث شد تشخیص نسخه نصب‌شده دشوار شود.

قانون دائمی:

- هر نسخه آزمایشی مهم باید نام برنامه، Package ID، Version Code و نام فایل منحصربه‌فرد داشته باشد.
- همراه Release باید `metadata.json` و `SHA256SUMS.txt` منتشر شود.
- نام برنامه، Package ID، Commit، SHA-256 و لینک Release باید هنگام تحویل اعلام شوند.
- نسخه‌های Pinggy و Tunnel قدیمی منسوخ‌اند و نباید دوباره پیشنهاد یا نصب شوند.

### ۶. تست ناکافی

بازبودن Process یا متفاوت‌بودن رنگ Screenshot با سفید، اثبات سلامت رابط نبود.

قانون دائمی:

- همان فایل APK که تحویل می‌شود فقط یک‌بار در CI ساخته و در تمام آزمایش‌ها استفاده شود.
- روی Android 13، 14 و 16 نصب و Cold Launch شود.
- متن واقعی فارسی و RTL با WebView/CDP بررسی شود.
- Screenshot صفحه واقعی ورود، بازیابی یا داشبورد ذخیره و بصری کنترل شود.
- اینترنت سالم، اینترنت قطع، DNS خراب، سرور خاموش، SSL نامعتبر و اجرای دوباره برنامه آزمایش شوند.
- Logcat از نظر Crash، خطای JavaScript، شبکه و WebView بررسی شود.
- اعلان دارای صدا، Alarm دقیق و لغو Alarm با تست رفتاری کنترل شوند.
- Release فقط پس از موفقیت تمام Jobهای اجباری منتشر شود.

نکته: Android 14 یک بار پس از Force Stopهای سریع با توقف گذرای Emulator روبه‌رو شد. اجرای دوباره همان APK بدون تغییر کد موفق بود. در چنین وضعی ابتدا همان Artifact دوباره آزمایش می‌شود تا خطای زیرساخت با خطای برنامه اشتباه نشود؛ هیچ کنترل اجباری حذف یا ضعیف نمی‌شود.

### ۷. نام فایل‌های مدرک

Screenshotهای چند نسخه Android اگر نام یکسان داشته باشند، هنگام انتشار با هم تداخل می‌کنند.

قانون دائمی:

- API Level باید در نام Screenshot، Log و Artifact نوشته شود.
- تمام مدارک هر اجرا با شماره Run و Commit قابل ردیابی باشند.

## چک‌لیست اجباری پیش از تحویل APK

- Commit لوکال، `BUILD_COMMIT.txt` و `/api/health` سرور، و `metadata.json` APK باید جداگانه تطبیق داده شوند. Push کد به‌تنهایی Staging یا APK را به‌روز نمی‌کند.
- آزمون رابط آفلاین باید علاوه بر متن فارسی، شعر ۳۶۰تایی، تاریخ میلادی، ایجاد سه اعلان بومی و لغو آن‌ها بعد از تکمیل کار را بررسی کند.
- ساخت بسته Linux هم‌نسخه با APK در Workflow پایدار انجام می‌شود. ارتقای Staging فقط با `scripts/deploy-staging-release.sh`، بررسی Hash و Backup انجام شود؛ بازگشت به Release قبلی داده‌های جدید کاربر را بازنویسی نمی‌کند.

- [ ] مقصد APK موقت و منقضی‌شونده نیست.
- [ ] رابط محلی خودکفا داخل APK وجود دارد.
- [ ] در خطای شبکه، DNS، SSL یا سرور صفحه سفید دیده نمی‌شود.
- [ ] APK تحویلی همان Artifact آزمایش‌شده است و دوباره Build نشده است.
- [ ] نام برنامه، Package ID، Version، Commit و SHA-256 ثبت شده‌اند.
- [ ] Android 13، 14 و 16 با متن واقعی فارسی و RTL موفق‌اند.
- [ ] Screenshot و Logcat واقعی بررسی شده‌اند.
- [ ] Type Check، Lint، Test، Build، Android Lint و تست‌های Android موفق‌اند.
- [ ] فایل محیطی، Secret، Keystore یا کلید امضا وارد Git نشده است.
- [ ] APK روی گوشی واقعی نصب و بازشدن آن ثبت شده است، یا این مورد صریحاً «معوق» اعلام شده است.
- [ ] Production و دامنه اصلی بدون تأیید نهایی مالک تغییر نکرده‌اند.

## Staging، Backup و Rollback

- Staging فعلی روی `https://personalagent.wealthos.ir:8443` از Production جداست.
- پیش از هر تغییر سرور، از دیتابیس، تنظیمات، Release فعال و وضعیت Processها Backup گرفته می‌شود.
- تغییر Production فقط با تأیید نهایی مالک انجام می‌شود.
- اگر GitHub Runner به پورت Staging دسترسی نداشت، تست رابط محلی و Recovery در Runner ادامه پیدا می‌کند و Smoke Test سرور جداگانه انجام می‌شود؛ بررسی‌ها حذف نمی‌شوند.

## خطاهای کشف‌شده در آزمون همسانی نسخه ۱۵ (۲۰۲۶-۰۹-۰۶ UTC)

- آزمون واقعی سه یادآوری روی هر سه Android شکست خورد: `Native notification bridge missing`. بازشدن رابط به‌تنهایی اثبات کارکرد اعلان نیست؛ نسخه ۱۵ منتشر نشد.
- Capacitor 8.5 صفحه `errorPath` را بدون تزریق پل افزونه‌ها تحویل می‌دهد؛ همچنین اسکریپت شروع سند در حالت متصل فقط برای مبدأ سرور فعال است. `MainActivity` پل استاندارد خود Capacitor را فقط در سند اصلیِ بازیابیِ داخل APK و فقط برای `LocalNotifications` تزریق می‌کند. مبدأها، اعتبارسنجی SSL و مجوزهای سیستم‌عامل تغییر نمی‌کنند.
- فونت باید از فایل داخل APK در مبدأ خصوصی `localhost` خوانده شود؛ مسیر دقیق فونت توسط برنامه سرو می‌شود. آزمون واقعی بارگیری Vazirmatn نیز الزامی است.
- آزمون باید سه اعلان بومی آینده را از فرم واقعی ایجاد کند، در `getPending` ببیند و پس از تکمیل کار لغوشدن آن‌ها را اثبات کند. این کنترل را برای سبزشدن CI حذف نکنید.
- کلید عمومی Web Push اکنون از API احرازهویت‌شده و در زمان اجرای سرور دریافت می‌شود؛ اتصال اعلان نباید به وجود تنظیمات Staging هنگام Build در GitHub وابسته باشد. کلید خصوصی فقط در سرور می‌ماند.
- آزمون نسخه ۱۶ نشان داد `String.replace` در Java تمام `<head>`ها، حتی نمونه داخل رشته JSON رابط جاسازی‌شده، را تغییر می‌دهد و SyntaxError ایجاد می‌کند. درج اسکریپت اکنون با برش در نخستین head واقعی انجام می‌شود؛ تست واحد، دست‌نخورده‌ماندن رشته HTML داخلی و `$` و backslash را کنترل می‌کند. نسخه ۱۶ منتشر نشد.
- نسخه ۱۷ دیگر SyntaxError نداشت، اما تست بارگیری فونت، NetworkError پیدا کرد. برخلاف نام متد، `Bridge.getLocalUrl()` در حالت متصل به مبدأ سرور اشاره می‌کند، نه `localhost`. مبدأ منابع داخل APK اکنون از `getScheme()` و `getHost()` ساخته می‌شود. این تفاوت باید در هر تغییر Capacitor بررسی شود؛ فونت خارج از APK یا نادیده‌گرفتن آزمون راه‌حل نیست. نسخه ۱۷ منتشر نشد.

## امنیت انتشار

- در آزمون نسخه ۱۸، Android 13 و 16 کامل موفق شدند؛ Android 14 پس از موفقیت فونت و سه اعلان، به‌علت مرگ `com.google.android.gms.persistent` بسته شد. Logcat صریحاً `depends on provider ... FontsProvider in dying proc` را ثبت کرد. این رخداد شبیه‌ساز را با خطای JavaScript برنامه اشتباه نگیرید؛ لاگ را نگه دارید و همان APK را دوباره آزمایش کنید. حذف آزمون آفلاین یا اعلان برای سبزشدن نتیجه مجاز نیست. تا موفقیت هر سه Android، نسخه قابل تحویل نیست.

- APK فعلی آزمایشی و Debug-signed است.
- Keystore، رمز امضا، `.env`، کلید API و اطلاعات شخصی هرگز وارد Git یا Artifact عمومی نمی‌شوند.
- پیامک و تماس تا انتخاب سرویس‌دهنده، شماره تأییدشده و تأیید هزینه در حالت Mock می‌مانند.
- محدودیت‌های Android، SSL، احراز هویت یا محدودیت کشوری دور زده نمی‌شوند.

## پیشگیری از بازگشت تنظیمات پیچیده — ۲۰۲۶-۰۹-۰۶

- تکرار روزانه/هفتگی برنامه با پیگیری هشدار پس از موعد متفاوت است. در وب و رابط آفلاین، فیلدهای تعداد/فاصله پیگیری تنها با روشن‌بودن `escalation` نمایش داده شوند؛ برچسب کانال `NATIVE` برابر `Notification` است و شناسه فنی آن تغییر نکرده است. تغییر منابع و صفحه بازیابی را معادل انتشار APK تازه حساب نکنید.
- `persian-inputs.ts` منبع مشترک تقویم/ساعت است؛ `input-controls.js` باید هم در assets و هم صفحه بازیابی جاسازی شود.
- تقویم انتخاب تاریخ باید ماه و روز شمسی واقعی و ساعت دقیقاً ۲۴ گزینه داشته باشد. بومی بودن کنترل time تضمین ۲۴ساعته بودن نیست.
- ثبت بدون ویرایش، عنوان فارسی، نبود عملیات/نوع تکراری/منطقه زمانی/مدت/سکوت، سه یادآوری و انصراف در آزمون WebView ثابت بمانند.
- پیش‌فرض داخلی جلسه ۶۰ دقیقه است؛ مدت جلسه قدیمی و هشدارهای قبلی حفظ شوند.
- گزارش نسخه و Hash فایل تحویلی جدا از وضعیت کد در `SIMPLIFIED_APPROVAL_2026-09-06.md` ثبت شود.
