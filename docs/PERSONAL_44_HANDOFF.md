# تحویل tia 1.0.44 — نسخه شخصی آزموده‌شده، ۲۶ سپتامبر ۲۰۲۶

**نسخه۴۴ برای آزمون شخصی منتشر شد؛ همان فایل امضاشده روی Android13/14/16 کامل موفق و۱۵تصویرش بازبینی شده است.** این نتیجه جای پذیرش گوشی مالک یا تکمیل الزامات سرور نیست؛ هیچ داده فعلی پاک نشده است. برنامه فعلاً نباید تنها محل اطلاعات حیاتی یا تنها هشدار کار حساس باشد.

[دانلود مستقیم tia 1.0.44](https://github.com/tparkhondeh/personalAgent/releases/download/personal-1.0.44/tia-1.0.44.apk) | [صفحه انتشار و مشخصات فایل](https://github.com/tparkhondeh/personalAgent/releases/tag/personal-1.0.44) | [وب آزمایشی](https://personalagent.wealthos.ir:8443/)

کاندید اولیه۴۴ در آزمون توقف اجباری بلافاصله بعد از اعلام ثبت، رکورد تازهٔ ساختگی را از دست داد و رد شد؛ هرگز منتشر نشد. در فایل تازه، موفقیت ثبت فقط بعد از تأیید ذخیره بومی اعلام می‌شود. مشخصات پایین متعلق به فایل اصلاح‌شده است؛ تاریخچه شکست و Hash فایل ردشده در گزارش بازبینی حفظ شده‌اند.

## نسخه‌ها و اطلاعات

- وب آزمایشی قابل بررسی: https://personalagent.wealthos.ir:8443/ — منبع `3b2b24028346ce50a22959de72cac8c5c28b95cc`.
- Production روی۴۴۳ تغییر نکرده و با این تحویل یکی نیست.
- APK اصلاح‌شده از منبع `6cd6e5902b9e1d47cda7f6b108c39b20165a87ce` است. اصلاح ابزار آزمون بعدی، بایت‌های APK یا نسخه سرور را تغییر نمی‌دهد.
- حالت متصل، اطلاعات حساب را در سرور ذخیره می‌کند؛ حالت آفلاین داخل APK مخزن جدا روی گوشی دارد و انتقال خودکار آن به حساب ادعا نمی‌شود. پشتیبان سرور داده فقط‌روی‌گوشی را پوشش نمی‌دهد.

## نصب و ارتقا

نسخه۴۴ همان نام، Package ID، امضای دائمی و مقصد۴۳ را دارد. حفظ اطلاعات ساختگی در ارتقای۴۳→۴۴، شروع سرد و ثبت واقعی همراه توقف فوری روی هر سه API آزموده شده است؛ پذیرش کامل فایل و تصاویر نیز موفق است. روی۴۳، فایل۴۴ را به‌صورت به‌روزرسانی نصب کنید، نه با حذف برنامه. این آزمایش، مشاهدهٔ اطلاعات گوشی مالک نیست.

نسخه آزمایشی۴۰ قدیمی امضای متفاوت دارد. نسخه نصب‌شده فعلی گوشی هنوز احراز نشده است؛ اگر Android خطای ناسازگاری داد، **برنامه را حذف و حافظه‌اش را پاک نکن**. اجازه تاریخی حذف داده آزمایشی، داده تازه امروز را شامل نمی‌شود. هیچ حذف یا نصب روی گوشی از این میزبان انجام نشده است.

## چک‌لیست یک‌باره گوشی مالک

1. وارد حساب شو؛ یک کار کوتاه ثبت کن، برنامه را ببند و دوباره باز کن. کار و تنظیمات باید باقی بمانند و شروع عادی روی «کارها» باشد.
2. Notification و Alarm نزدیک را با صفحه قفل بررسی کن. سپس همان کار را انجام‌شده کن؛ هشدار آینده‌اش نباید دوباره برسد. مجوز و شدت صدا را روی گوشی بررسی کن.
3. صدای نمونه را به پیشنهاد قابل‌ویرایش تبدیل کن؛ جداگانه با رضایت ارسال متن ساختگی، نشان «GPT متصل» و اصلاح همان پیشنهاد را بررسی کن. هیچ موردی پیش از «ثبت» ساخته نشود. مجوز ارسال صدای شخصی به سرویس بیرونی وجود ندارد.

## موانع ضروری باقی‌مانده

| مورد | اقدام و مسئول |
|---|---|
| پذیرش گوشی واقعی | سه بررسی بالا با مالک؛ ADB این میزبان دستگاهی نشان نمی‌دهد. آزمون شبیه‌ساز جای آن نیست. |
| بازیابی ایمیلی حساب | API تازه `available:false`؛ مدیر/مالک باید سرویس ایمیل مجاز را فراهم کند. کلید یا هزینه تازه ساخته/فعال نشده است. |
| ظرفیت سرور | ساعت12:28UTC حدود۲٫۷۲GiB آزاد و۹۹٪مصرف؛ مدیر ظرفیت پایدار فراهم کند. حذف فایل دیگر پروژه‌ها یا پشتیبان‌ها مجاز نیست. |
| پشتیبان خارج سرور | زمان‌بندی روزانه محفوظ است و اجرای تازه ابزار و بازیابی جدا روی همان سرور موفق‌اند؛ اجرای زمان‌بندی‌شده بعدی هنوز در این نوبت مشاهده نشده است. مالک باید مقصد امن و روش نگهداری کلید را تعیین کند؛ انتقال اطلاعات خصوصی بدون رضایت انجام نشده است. |
| نگهداری امضا | همان کلید دائمی و پشتیبان خصوصی حفظ شده‌اند؛ بازیابی خارج پروفایل ویندوز فعلی اثبات نشده است. مالک باید نسخه امن خارج دستگاه و روش بازیابی را فراهم کند. |

تا رفع این محدودیت‌ها، برنامه را تنها محل نگهداری اطلاعات حیاتی یا تنها راه هشدار یک کار حساس ندانید. این گزارش تضمین نبود خطا نیست.

## GPT و هزینه

دو پاسخ واقعی تازه از مسیر عمومی سرور موفق شدند: عنوان و ساعت، سه یادآوری، اصلاح همان پیشنهاد، ثبت با تأیید و جلوگیری از تکرار. بدون رضایت متن، حالت محلی فعال ماند؛ انصراف و لغو هشدار بعد از تکمیل موفق بودند. این آزمون روی حساب ساختگی جدا بود و در پایان مجوز آن برداشته شد.

تنها دفتر نسخه۲ سرور فعال است؛ هزینه آزمون۰٫۰۰۳۹۰۹دلار و مجموع ماه۰٫۱۷۱۸۹۵دلار از۲دلار ثبت شده، بدون رزرو معلق. این رقم دفتر برنامه است، نه صورتحساب قطعی OpenAI. لوکال بودجه مستقل ندارد؛ صدای شخصی ارسال نشده است. GPT اینترنتی در حالت آفلاین فعال نیست.

## آزمون‌های موفق و مرز آن‌ها

- [CI وب۳۶۲۴۰۸۷۳۷۰۵](https://github.com/tparkhondeh/personalAgent/actions/runs/36240873705):۱۴۴۶تست در۱۰۵فایل، Type Check، Lint و Build؛ مرورگر واقعی دسکتاپ/موبایل نیز بررسی شد.
- Android13: شاخه موفق [۳۶۲۳۹۳۱۳۷۱۶](https://github.com/tparkhondeh/personalAgent/actions/runs/36239313716)، QA `0576c92788252cc92658fe7cac2bc2fb75a2f6b5`. آن اجرای کلی به علت۱۴/۱۶ ناموفق بود؛ فقط شاهد۱۳ بازاستفاده شده است.
- Android14/16: [۳۶۲۴۰۹۰۱۷۵۱](https://github.com/tparkhondeh/personalAgent/actions/runs/36240901751)، QA `a970e402b3157538dfc44f306cc59648c7f71790`، هر دو کامل موفق.
- ۲۳تست بومی در هرکدام، ارتقای۴۳→۴۴، حفظ همان شناسه/هش پس از ثبت UI و توقف فوری، سه یادآوری و لغو آینده/تحویل‌شده، صوت نمونه محلی،۳۶۰شعر، بازیابی اتصال و شروع عادی بدون debugging موفق‌اند. کران بالای توقف فوری۱۳/۱۴/۱۶ به‌ترتیب۱۶۱۸/۱۷۵۵/۴۷۸میلی‌ثانیه است؛ این اعداد سرعت واقعی دیسک نیستند.
- ۱۵تصویر واقعی، شامل روشن/تیره، کیبورد، بازیابی و ارتقا بررسی شدند؛ پنجره خطای سیستم روی تصویر نبود. کد محصول و امضای فایل در این آزمون‌ها یکسان‌اند.
- شبکه CI به۸۴۴۳ دسترسی عمومی نداد؛ بنابراین آزمون بازیابی و GPT واقعی سرور، اثبات GPT متصل Android یا گوشی مالک نیست. این مورد با آزمون شبکه و گوشی مالک تکمیل می‌شود.

## مشخصات فایل منتشرشده

| مشخصه | مقدار |
|---|---|
| نام/نسخه | tia / 1.0.44 / code44 |
| Package ID | `ir.wealthos.personalagent.stable40` |
| Commit سازنده APK | `6cd6e5902b9e1d47cda7f6b108c39b20165a87ce` |
| اندازه | 59,368,563 bytes |
| SHA-256 APK | `fa62f5b0f9e55270fc78f01dc16441162a7ba0e13337a81d8c24d2fcbfbfa377` |
| SHA-256 گواهی امضا | `abfd097ac3ab887977fb58505b5ae0e40c1fc5b56bf210597476105278f1e3cf` |

دانلود دوباره و بدون ورود از لینک عمومی GitHub در12:29UTC با TLS معتبر انجام شد؛ اندازه و SHA دقیقاً با همان فایل آزموده‌شده برابر بودند. کلید، testAPK و فایل‌های تشخیصی در انتشار نیستند.

## بازگشت امن

قبل از استقرار Staging، داده‌ها و دفتر هزینه جدا بازیابی شدند. نسخه کد قبلی `8dcfea6e3f9f42d871c0c864c0206e2d3de1575b` محفوظ است؛ بازگشت فقط کد، پس از کنترل سازگاری است. پایگاه داده یا دفتر هزینه به عقب بازگردانده نشوند. روی گوشی، downgrade اجباری یا حذف برنامه راه بازگشت نیست؛ نسخه اصلاحی هم‌امضا با شماره بالاتر ترجیح دارد.

[جزئیات بازبینی چهار ایجنت و شواهد آزمون](FINAL_REVIEW_2026-09-26.md)
