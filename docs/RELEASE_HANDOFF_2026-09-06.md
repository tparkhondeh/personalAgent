# تحویل و مقایسه نسخه‌ها — ۲۰۲۶-۰۹-۰۶ UTC

## نتیجه ممیزی اولیه

- پوشه اصلی روی شاخه `main` بود و Commit محلی و GitHub هر دو `df2757b` بودند؛ بازگشت کد مشاهده نشد.
- Worktree قدیمی `personal-agent-phone-preview-e215c3a` جدا بود و دست‌نخورده حفظ شد.
- Staging و سرویس داخلی Production هر دو هنوز از Release قدیمی `49cceed` استفاده می‌کردند.
- آخرین APK منتشرشده، نسخه ۱۴ از `e26a8f0` بود؛ تغییرات تازه وب به رابط آفلاین آن نرسیده بود.
- بنابراین تفاوت نسخه‌ها واقعی بود، اما علتش انتشارنشدن تغییرات تازه در سرور/APK بود، نه ازبین‌رفتن آن‌ها در Git.

## وضعیت تحویل

- لوکال: <http://localhost:3001> از همین پوشه اصلی اجرا شده است.
- وب آزمایشی پایدار: <https://personalagent.wealthos.ir:8443>؛ وابستگی به تونل موقت ندارد.
- Staging اکنون `17afb3d5e55ce984fc663068902db7125e9f7d77` است؛ Health، ورود در مرورگر، سه یادآوری، Task/Meeting، اعلان، Escalation و دستیار محلی موفق‌اند.
- [دانلود مستقیم و پایدار APK شماره ۱۹](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-19/Hamrah-stable-19.apk)؛ [صفحه Release و تصاویر آزمون](https://github.com/tparkhondeh/personalAgent/releases/tag/phone-preview-stable-19).
- نام دقیق برنامه: **همراه پایدار 19**؛ Package ID: `ir.wealthos.personalagent.stable19`؛ Version: `1.0.19`، Code: `19`.
- Commit سازنده: `17afb3d5e55ce984fc663068902db7125e9f7d77`؛ زمان انتشار: `2026-09-06T03:27:18Z`.
- SHA-256: `bb396ba1be0f0efddc2073facedae3670eb9ba0b032c5ac75e5622d79cfac566`؛ حجم: ۴٬۴۶۲٬۳۵۴ بایت.
- فایل منتشرشده دوباره از لینک دائمی دانلود شد: HTTP 200، هش دقیقاً برابر فایل آزموده‌شده و Digest دارایی GitHub. فایل پس از آزمون دوباره Build نشده است.
- کاندیدهای ۱۵ تا ۱۸ منتشر نشدند؛ ۱۹ تمام کنترل‌های سه Android را گذراند. این نسخه همچنان آزمایشی و Debug-signed است، نه نسخه عمومی نهایی.

| قابلیت | کد و لوکال | Staging | APK منتشرشده ۱۹، حالت متصل | رابط آفلاین داخل APK ۱۹ |
|---|---|---|---|---|
| فونت فارسی و پاستل | اعمال شده | اعمال شده | اعمال شده از Staging هم‌نسخه | اعمال شده؛ فایل فونت داخل APK واقعاً بارگذاری شد |
| شعر مشترک ۳۶۰تایی، منبع مخفی و دو تاریخ | اعمال شده | اعمال شده | اعمال شده از سرور | اعمال شده؛ همسانی محتوای فایل و متن فارسی آزموده شد |
| همراه ساده و تب‌های سالم | اعمال شده | اعمال شده | اعمال شده از سرور | اعمال شده؛ دستیار خلاصه‌ساز محلی، نه LLM |
| سه یادآوری هم‌زمان | اعمال شده | اعمال شده | تنظیم وب اعمال شده | اعمال شده؛ ایجاد سه اعلان بومی و لغو پس از تکمیل آزموده شد |
| LLM واقعی | فعال‌سازی خارجی باقی مانده | فعال‌سازی خارجی باقی مانده | فعال‌سازی خارجی باقی مانده | فقط دستیار محلی؛ کلیدی در APK نیست |
| تماس خروجی واقعی | فعال‌سازی خارجی باقی مانده | فعال‌سازی خارجی باقی مانده | فعال‌سازی خارجی باقی مانده | تماس واقعی ندارد |

حالت متصل همان رابط وب Staging را نمایش می‌دهد. حالت آفلاین، رابط ساده‌تر و مستقل داخل APK است؛ داده‌های آن فقط روی دستگاه ذخیره می‌شوند و هنوز با حساب سرور همگام نمی‌شوند. آماده‌بودن رابط یا کد، اثبات فعال‌بودن LLM، تماس یا تحویل واقعی Push به گوشی نیست.

نسخه ۱۹ کنار نسخه ۱۴ نصب می‌شود؛ نسخه قبلی را برای این آزمایش حذف نکنید. داده‌های محلی نسخه قبلی خودکار به Package تازه منتقل نمی‌شوند؛ اطلاعات حساب سرور پس از ورود از همان Staging خوانده می‌شوند. پوشه اصلی پس از `17afb3d` فقط مستندات تحویل را جلو برده است؛ تفاوت Commit مستندات با Commit APK به معنی قدیمی‌بودن کد برنامه نیست.

## شواهد کیفیت

- [اجرای کامل و موفق 34008659255](https://github.com/tparkhondeh/personalAgent/actions/runs/34008659255): Type Check، Lint، هر ۶۰ تست وب، Build و Audit موفق‌اند.
- Android Lint بدون issue؛ هر ۴ تست واحد شامل سه تست پیشگیری از خرابی HTML موفق‌اند.
- Android 13، 14 و 16 هرکدام هشت تست بومی را گذراندند. متن واقعی فارسی، RTL، فونت Vazirmatn، شعر ۳۶۰تایی، تاریخ میلادی، سه اعلان Native و لغو آن‌ها در هر سه نسخه موفق‌اند. Screenshotهای واقعی داشبورد و Recovery در هر سه نسخه بصری بررسی شدند؛ Logcat خطای JavaScript یا Crash برنامه نشان نداد. خطاهای عمدی شبکه و لاگ سرویس‌های گوگل از خطای برنامه تفکیک شدند.
- همان APK تحویلی در Cold Launch، قطع اینترنت، اجرای دوباره و اتصال مجدد آزمایش شد. خطاهای هدفمند DNS، سرور خاموش و SSL با سه APK تشخیصی ساخته‌شده از همان کد و مقصد خطای کنترل‌شده آزموده شدند؛ آن سه فایل، APK تحویلی نیستند و منتشر نشدند. شبکه Runner به پورت ۸۴۴۳ دسترسی نداشت، بنابراین رابط متصل در مرورگر واقعی Staging بررسی شد، نه به‌عنوان تست موفق اتصال زنده در شبیه‌ساز گزارش شود.
- شواهد محلی: `backups/local/android19-33`، `android19-34` و `android19-36`؛ APK و Metadata در `backups/local/stable19-build`. تصاویر در GitHub Release نیز هستند؛ Artifact کامل CI نگهداری محدود ۱۴روزه دارد.
- محتوای `app.js`، `app.css`، `domain.js`، `content.js` و `connection-error.html` استخراج‌شده از APK با منبع فعلی تطبیق داده شد. مقصد HTTPS پایدار، Package، Version، نبود فایل محیطی/Keystore و SHA نیز بررسی شدند.
- نسخه ۱۵ به‌علت نبود پل اعلان، ۱۶ به‌علت SyntaxError در تزریق HTML، ۱۷ به‌علت مبدأ اشتباه فونت و ۱۸ به‌علت مرگ وابسته به سرویس گوگل در شبیه‌ساز ۱۴ تحویل نشدند. علت‌ها و پیشگیری در `ANDROID_RELEASE_PLAYBOOK.md` ثبت شده‌اند. هیچ آزمونی برای سبزشدن حذف نشد.
- نمای واقعی وب روی دسکتاپ و موبایل ۳۹۰پیکسلی، متن فارسی، نبود سرریز افقی و کارکرد تب همراه بررسی شد. دکمه ابزار توسعه که روی تب همراه قرار می‌گرفت حذف شد.
- Smoke لوکال و Staging: `llm-local,call-mock`؛ هیچ تماس یا پیامک واقعی ارسال نشده است.
- تنظیم عمومی Push روی Staging در زمان اجرا قابل دریافت است؛ تحویل Push روی گوشی واقعی این نسخه هنوز باید آزمایش شود.
- تأیید قبلی گوشی مربوط به نسخه ۱۴ است؛ آن را به نسخه تازه تعمیم نمی‌دهیم.
- جداسازی حساب در `17afb3d` اصلاح و عملاً بررسی شد: کار مصنوعی حساب QA ساخته شد؛ پس از خروج و Reload مهمان نه عنوان کار و نه Badge اعلان خصوصی دیده شد؛ پس از ورود دوباره همان کار و اعلان‌ها برگشتند. کار آزمایشی تکمیل شد و برای سابقه حفظ شد. کارها، تنظیمات و ورود حساب واقعی مالک در مرورگر لوکال دست‌نخورده ماندند.
- Worker مستقل `personal-agent-staging-scheduler` فعال شد؛ چند چرخه موفق یک‌دقیقه‌ای برای یادآوری و Escalation در لاگ خصوصی ثبت شد. بدنه پاسخ، اطلاعات کاربر و کلید در لاگ نوشته نمی‌شوند؛ Secret در آرگومان Process نیز قرار نمی‌گیرد. توقف این Worker و ذخیره وضعیت PM2، روش بازگشت این تغییر است؛ Cron اصلی دست‌نخورده مانده است.

## Backup و بازگشت

- پشتیبان کامل کد/Git، تغییرات و Worktree قدیمی: `C:/Users/pc/Desktop/project-backups/personal-agent-20260905-191617`؛ این پشتیبان خصوصی است و نباید منتشر شود.
- دیتابیس لوکال: Snapshot سالم `hamrah-2026-09-06T02-16-28-764Z.db` در `backups/local`.
- Backup مقدماتی سرور: `pre-parity-20260906T022211Z` در پوشه Backupهای خصوصی Staging.
- پیش از ارتقای Staging: `/home/wealthos_dev/.staging/personal-agent/data/backups/pre-release-20260906T024932Z` شامل دیتابیس سالم، تنظیمات و وضعیت Processها.
- ارتقای بعدی به `833c67b`: Backup سالم `pre-release-20260906T030046Z`، نسخه بازگشت `af265dc`؛ پیش از Worker نیز `pre-scheduler-20260906T025852Z` شامل تنظیمات، PM2 و Cron موجود ذخیره شد.
- ارتقای نهایی Staging به `17afb3d`: Backup سالم `pre-release-20260906T032354Z`؛ نسخه بازگشت `/home/wealthos_dev/.staging/personal-agent/releases/833c67ba9676cd048013fd8a68ce0d6a72e37b67`. بسته سرور SHA-256: `12624fd847b1cda082e54acb91a5400107c5c53726af8403c8292a38fc8d34e7`.
- Migration نخست روی کپی دیتابیس آزموده شد؛ فقط ستون یادآوری چندگانه اضافه شد. داده کاربر بازنویسی نشده است.
- بازگشت Staging با تغییر Symlink به Release قبلی و Restart فقط `personal-agent-staging` است؛ دیتابیس جدید با Snapshot قدیمی جایگزین نمی‌شود. Script پس از خطای Health این بازگشت را خودکار انجام می‌دهد.
- نسخه قبلی `833c67b` اصلاح تازه جداسازی رابط پس از خروج از حساب را ندارد؛ بازگشت به آن فقط راه اضطراری راه‌اندازی است، نه نسخه پیشنهادی استفاده روزمره. پس از بازگشت اضطراری، دسترسی آزمایشی محدود و اصلاح رو به جلو بررسی شود.

## مراحل بعدی و Production

| اولویت | اقدام | زمان مشروط |
|---|---|---|
| انجام شد | APK هم‌نسخه، سه Android، بررسی تصاویر، دانلود و تطبیق هش، Staging و Smoke | تکمیل در این نوبت |
| ۱ | بررسی نسخه ۱۹ روی گوشی مالک، بازکردن تب‌ها و اجازه/تحویل اعلان و Alarm | حدود ۱۰–۲۰ دقیقه از زمان نصب و در دسترس بودن گوشی |
| ۲ | فعال‌سازی LLM یا تماس، فقط با کلید امن و تأیید هزینه/شماره | بعد از آماده‌شدن سرویس؛ زمان تأمین حساب قابل تضمین نیست |
| ۳ | تعیین راه افزایش ظرفیت سرور، بررسی پنل/Proxy و انتشار عمومی | فقط پس از پذیرش نسخه و تأیید نهایی؛ حدود ۳۰–۶۰ دقیقه برای انتقال اگر همه پیش‌نیازها آماده باشند |

Production و Reverse Proxy دامنه اصلی تغییر نکرده‌اند: ریشه دامنه پاسخ ۲۰۰ و `/api/health` پاسخ قبلی ۴۰۴ می‌دهد؛ سرویس داخلی Production روی Release `49cceed` باقی است. پس از پشتیبان‌ها و ارتقا، فضای خالی دیسک حدود **۱٫۳ گیگابایت** است و `df` مصرف ۱۰۰٪ گرد‌شده نشان می‌دهد؛ پیش از انتشار عمومی باید ظرفیت کافی فراهم شود. هیچ فایل یا Backup برای خالی‌کردن فضا حذف نشده است.

خطرهای انتشار عمومی: تنظیم اشتباه Proxy/HTTPS، Cookie و ورود، کمبود فضای دیسک و ناسازگاری Migration. پیش از هر تغییر، Backup تازه و مقصد قبلی Proxy ثبت می‌شود؛ در خطا، Proxy و نسخه برنامه برمی‌گردند. بازیابی دیتابیسِ قدیمی روی داده تازه بدون بررسی و تأیید جداگانه انجام نمی‌شود. هیچ تغییر Production در این نوبت مجاز یا انجام‌شده نیست.
