# چهار باکس و شعر بعدی — tia 38

## وضعیت شواهد

- کد و رابط داخل APK: `e0adc38fd50842877451e831de9bda1431de27e7`، روی main و GitHub.
- لوکال همین پوشه: [نمای وب](http://localhost:3001/)، آزمون واقعی در عرض‌های ۱۲۸۰، ۳۹۰ و ۳۲۰ پیکسل؛ روشن/تیره، حفظ شعر پس از بارگذاری مجدد و تغییر تب، ساخت و تکمیل و حذف جلسه مصنوعی موفق.
- Staging: [محیط آزمایشی](https://personalagent.wealthos.ir:8443/) از همان Commit؛ سلامت HTTPS و آزمون‌های کاربردی از خود سرور موفق. دسترسی HTTPS از میزبان توسعه با خطای handshake و مرورگر با timeout مواجه شد؛ اتصال روی همه شبکه‌ها تأیید نشده و علت شبکه/مسیر اتصال قطعی تعیین نشده است. DNS/Proxy یا تنظیم TLS برای دورزدن مشکل تغییر نکرد.
- [دانلود tia آزمایشی 38](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-38/tia-stable-38.apk). همان فایل بدون بازسازی منتشر شد؛ digest هر ۱۵ فایل انتشار (APK، مشخصات، SHA و ۱۲ تصویر) با فایل محلی برابر بود. لینک عمومی بدون احراز هویت دوباره روی سرور دریافت شد و SHA دقیقاً برابر بود.
- Android متصل، منبع UI را از Staging می‌گیرد؛ runner به آن نرسید و بازیابی/رابط بسته‌بندی‌شده آزموده شد. این نتیجه را معادل پذیرش آنلاین گوشی مالک نگیرید.
- Production تغییر نکرده؛ هیچ سرویس پولی یا کلید تازه فعال نشده است.

## هویت همان فایل آزموده‌شده

- نام: `tia آزمایشی 38`
- Package ID: `ir.wealthos.personalagent.stable38`
- نسخه: `1.0.38`، versionCode: `38`؛ شناسه و نام با aapt از خود فایل کنترل شد.
- فایل: `tia-stable-38.apk`، اندازه `60052061` بایت.
- SHA-256: `343e6931ac74c926a226aeb2856b052d7c122ae4723652d606e7bc3d6c766e86`
- [اجرای ساخت و آزمون همان APK](https://github.com/tparkhondeh/personalAgent/actions/runs/34224042971)، هر سه Android موفق؛ انتشار خودکار عمداً غیرفعال بود تا تصاویر بررسی شوند.

## رفتار نهایی این مرحله

- چهار باکس دقیق `ALL TASKS / PERSONAL / BUSINESS / MEETING` در امروز و کارها. عدد بزرگ شامل تمام وضعیت‌های فهرست/فیلتر فعلی است؛ زیر آن تعداد انجام‌شده‌ها نمایش داده می‌شود.
- جلسه فقط در MEETING و مجموع است، نه دوباره در شخصی/شرکتی. دسته قدیمی جلسه در داده محفوظ است.
- امروز فقط تاریخ روز تهران را نشان می‌دهد. موارد بدون تاریخ، عقب‌افتاده و آینده حذف نشده‌اند و در کارها هستند. تکمیل، تعداد کل را کم نمی‌کند؛ شمارنده انجام‌شده را تغییر می‌دهد.
- شعر بعدی از مجموعه ۳۶۰ شعر موجود است. انتخاب در همان روز محفوظ می‌ماند؛ روز بعد به چرخه معمول برمی‌گردد. ذخیره‌سازی مسدود باشد، انتخاب تا زمان بازبودن صفحه در حافظه می‌ماند؛ اطلاعات شخصی ذخیره نمی‌شود.
- چهار مصراع در دو ستون RTL، رنگ‌ها از منبع مشترک، پیش‌فرض روشن مستقل از سیستم، کنترل لمسی ۴۴ پیکسلی و اسکرول لازم صفحه کوچک حفظ شدند.

## ایرادهایی که واقعاً رفع شدند

1. منطق متفاوت محدوده امروز و تکمیل‌شده‌ها در وب و رابط داخلی با ماژول مشترک جایگزین شد؛ تست، خود خروجی JavaScript تولیدشده برای Android را نیز اجرا می‌کند.
2. تکمیل جلسه وب قبلاً عملی انجام نمی‌داد و اعتبارسنجی API وضعیت را نگه نمی‌داشت؛ تکمیل/بازگردانی امن، کنترل دوبارکلیک و بازیابی شکست اضافه شد.
3. کنترل تاریخ/ساعت آفلاین فقط هنگام change مقدار را می‌گرفت؛ هنگام تایپ و ثبت فوری ممکن بود مقدار ثبت نشود. رویداد input نیز پشتیبانی و در مرورگر/Android آزموده شد.
4. کم‌رنگی کل ردیف تکمیل‌شده خوانایی را کاهش می‌داد؛ خط‌خوردگی باقی ماند ولی کنتراست متن حفظ شد.

## آزمون‌ها

- Type Check، Lint، Build وب و ۲۵۶ تست در ۳۷ فایل موفق؛ audit وابستگی‌های اجرایی آسیب‌پذیری شناخته‌شده گزارش نکرد.
- آزمون HTTP لوکال و HTTPS روی سرور Staging: ورود فارسی، ساخت/حذف کار، ساخت/تکمیل/بازگردانی/حذف جلسه، سه یادآوری، اعلان، دستیار محلی و سه Alarm برنامه‌ریزی‌شده موفق.
- ۲۷ کنترل تأیید/مالکیت/اصلاح/عدم تکرار، ۸ کنترل حفظ زمان‌بندی و ۷ کنترل ورود و نشست روی لوکال و Staging موفق. فقط حساب‌های مصنوعی استفاده شدند.
- Android Lint، تست واحد و ساخت در همان اجرای CI موفق؛ ۱۱ تست بومی در هر Android 13/14/16 موفق.
- برای هر Android، پنج گزارش مستقل چهار باکس/فیلتر/CRUD/شعر بعدی/حفظ انتخاب موفق؛ آزمون‌های قبلی تطبیق UI، تاریخ شمسی، ساعت ۲۴ساعته، سه اعلان محلی، انصراف، ثبت پس از تأیید، ورودی صوت و کیبورد حفظ شدند.
- نمونه صوت فارسی عمومی واقعاً به «خوش آمدید» رسید و از MediaRecorder به پیشنهاد تحویل شد؛ صدای خارجی ارسال نشده و قبل از تأیید اثری ساخته نشد. این آزمون میکروفون مصنوعی است، نه کیفیت میکروفون فیزیکی مالک.
- Cold Launch، قطع اینترنت، DNS نامعتبر، سرور خاموش، SSL نامعتبر، انتخاب تم و بازیابی، UI سطح سیستم و Logcat در هر سه Android موفق.
- ۱۲ تصویر واقعی (رابط روشن چهار باکس، تنظیمات تیره، بازیابی و کیبورد در هر سیستم) بصری بررسی شدند؛ خطای سیستم روی تصاویر نبود. ثبت و ورودی با کیبورد باز قابل مشاهده‌اند.
- تغییر روز شعر و مرز تهران با ساعت کنترل‌شده در تست واحد بررسی شد؛ برای آن ادعای گذشت واقعی یک شب روی گوشی نداریم.

## پشتیبان و بازگشت

- پیش از تغییر: `C:/Users/pc/Desktop/project-backups/personal-agent-dashboard-20260908/source-history.bundle`، اعتبارسنجی کامل؛ SHA `930d383ce5cfe95ea8b364fa5f22bf67ef52b598c5be18b212f3c195a1ba350a`.
- SQLite سازگار: `backups/local/hamrah-2026-09-08T11-18-59-383Z.db` با integrity سالم؛ تنظیمات خصوصی خارج Git نگهداری شد.
- Staging پیش از استقرار: `/home/wealthos_dev/.staging/personal-agent/data/backups/pre-release-20260908T125328Z`؛ دیتابیس با SQLite backup، تنظیمات و نشانگر نسخه قبلی محفوظ؛ migration روی کپی بررسی شد.
- بسته سرور SHA: `d1f0c4e346617a1417f795e74a1db2f0bffce922e75edcc09fe74ffec1a7b077`.
- نسخه بازگشت: `4a13c175756696908871f41748951d6da8ac8e4b`. در خطای تأییدشده، زمان‌بند Staging متوقف و نشانگر سرویس فقط به این نسخه برگردد؛ دیتابیس قدیمی روی داده تازه بازنویسی نشود. نیاز به رفع رو به جلو و بررسی سیاست هشدارها وجود دارد.
- آزمون‌ها پشتیبان یا نسخه‌های قدیمی را پاک نکردند. APK شناسه مستقل دارد؛ نسخه قبلی گوشی را حذف نکنید، زیرا داده محلی خودکار منتقل نمی‌شود.

## موارد باقی‌مانده واقعی

| مورد | وضعیت/وابستگی |
| --- | --- |
| پذیرش همین نسخه روی گوشی مالک | اتصال شبکه، میکروفون واقعی و رفتار اعلان/Alarm باید روی گوشی بررسی شود |
| LLM خارجی | برنامه‌ریز محلی فعال؛ کلید معتبر و رضایت هزینه خارجی باقی است |
| Push | تنظیم سرور موجود است؛ تحویل واقعی به گوشی هنوز تأیید نشده |
| تماس و پیامک | فقط Mock، بدون تماس/هزینه واقعی |
| Production | تغییر نکرده؛ پذیرش گوشی، بررسی مسیر اتصال و تأیید نهایی مالک لازم است |

این اصلاح مستقل آماده آزمون است؛ کامل‌بودن کل محصول یا همه سرویس‌های خارجی ادعا نمی‌شود.
