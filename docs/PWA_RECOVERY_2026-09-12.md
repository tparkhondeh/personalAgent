# کنترل نهایی و بازیابی امن وب — ۱۲ سپتامبر ۲۰۲۶

## مبنا و حفظ اطلاعات

main محلی و GitHub: `a30264b3a4569bc552e2293c91c1aeba3df6b6e0`، درخت تمیز. Staging در `2026-09-12T13:38:27.138Z` با کد `eb6193e8391c9049e04c1bc88aa2094b3945a7bf` و دیتابیس سالم فعال بود. آخرین APK همچنان `phone-preview-stable-40` است؛ هیچ نسخه تازه‌ای ساخته نشده است.

پشتیبان پیش از تغییر: `C:/Users/pc/Desktop/project-backups/tia-readiness-20260912`؛ تمام refs در bundle و کد در source.zip، env خصوصی جدا. bundle verify موفق؛ SHA آرشیو کد `2a6d0c24c3da8b3692080e68d4a1d02df4cb842bdc39ebc7408850119443d380`. Snapshot سازگار SQLite با integrity موفق: `backups/local/hamrah-2026-09-12T13-39-03-636Z.db`. داده و تاریخچه حذف نشدند.

تمرین بازیابی جدید روی `backups/local/restore-drill-jehOIo/restored.db`: integrity/foreign keys موفق و هر ۱۸ جدول قبلی پس از migration روی کپی بدون تغییر. SHA snapshot: `93988089ce17fdec4ae775a3c3d9052db2ad6129399cabdff3ce349e8a7e661f`. هیچ زمان‌بند یا ارسال خارجی در تمرین اجرا نشد.

## علت قابل اثبات و اصلاح

- در worker قبلی، شکست شبکه به `caches.match(request)` و سپس ریشه ذخیره‌شده برمی‌گشت؛ هیچ علامت قدیمی/آفلاین وجود نداشت. این مسیر می‌توانست رابط قدیمی را سالم جلوه دهد. علت زیرساختی اتصال قبلی از روی آن قطعی نیست.
- worker جدید سند تعاملی قدیمی را fallback نمی‌کند؛ خطای شبکه، پایان انتظار ۱۲ ثانیه یا 5xx به صفحه فارسی مستقل با «تلاش دوباره» و HTTP 503 می‌رسد. ورود و خطاهای 401/403/404 دست‌نخورده‌اند. Retry در فرم فعال یا هنگام کار کاربر خودکار اجرا نمی‌شود.
- صفحه بازیابی بدون React/شبکه/Cache Storage قابل نمایش است؛ وزیرمتن، پالت و انتخاب روشن/تیره از منابع مشترک تولید می‌شوند. CSP محدود با hash دقیق اسکریپت ظاهر؛ هیچ کلید، شماره، درخواست یا داده حساب در آن نیست.
- اسناد حساب، API، درخواست تغییردهنده و RSC وارد کش نمی‌شوند. فقط منابع ثابت موفق ذخیره می‌شوند؛ خرابی/پرشدن کش، پاسخ سالم شبکه را خراب نمی‌کند. هیچ کش/کوکی/نشست یا Web Storage قدیمی حذف نمی‌شود.
- `sw.js` و اسکریپت بازیابی از کش HTTP استفاده نمی‌کنند. فایل تولیدی در Build بازتولید و در تست با منبع مشترک تطبیق می‌شود؛ CRLF ویندوز به LF نرمال می‌شود.
- این اصلاح، ویرایش کامل آفلاین در PWA ایجاد نمی‌کند. نسخه وبِ حساب‌دار برای کار با سرور به اتصال نیاز دارد؛ حالت مستقل آفلاین داخل APK جداست و تغییر نکرده است. `BridgeWebViewClient` فعلی Capacitor در خطای HTTP سند اصلی به errorPath بومی می‌رود؛ مسیر موجود بدون تغییر امنیت/مقصد حفظ شد، اما آزمون واقعی گوشی همچنان لازم است.

## شواهد آزمون

- ۳۲۰ تست در ۴۳ فایل موفق؛ ۱۷ تست worker شامل ۱۵ تست رفتاری تازه. Type Check نهایی، Lint و Build جداگانه موفق؛ یک خطای نوع در ابزار آزمون قبل از تحویل رفع شد.
- HTTP لوکال: ۲۷ تأیید/انصراف/جلوگیری از تکرار + ۸ حفظ هشدار + ۷ ورود/نشست + ۲۳ تکمیل/آمار/لغو هشدار = ۶۵ کنترل موفق؛ فقط حساب و رکورد ساختگی.
- مرورگر واقعی، مبدأ جدا `127.0.0.2:3013`: worker واقعی و نسخه قدیمی ساختگی cache نصب شد؛ قطع پاسخ TCP و HTTP 503 صفحه بازیابی نشان دادند، Retry به نسخه تازه برگشت، سپس sentinel داده و کش قبلی حفظ‌شده تأیید شدند. پاسخ کند ۱۸ ثانیه‌ای نیز به بازیابی رسید؛ ابزار مرورگر هنگام انتظار طولانی timeout داشت، بنابراین اندازه‌گیری میلی‌ثانیه‌ای آن از ابزار ادعا نمی‌شود.
- تصویر دسکتاپ روشن و تصویر موبایل تیره بصری بررسی شدند. DOM موبایل: ۳۹۰×۸۴۴، فونت متن ۱۵px، عرض محتوا ۳۹۰، بدون اسکرول افقی؛ تم تیره فقط با انتخاب ذخیره‌شده آزمون فعال شد. هیچ داده مرورگر اصلی برای آزمون پاک نشد.
- لوکال از همین پوشه دوباره اجرا شد. چهار باکس و شعر بعدی حاضر؛ تایپ فارسی و الزام ورود قبل از ارسال پیشنهاد در مرورگر واقعی موفق. صوت ضبط نشد و هیچ داده شخصی به بیرون نرفت.
- نمای خود داشبورد هم با اندازه واقعی ۳۹۰×۸۴۴ و تصویر بررسی شد: چهار باکس، شعر و ناوبری پایین حاضر؛ بدون اسکرول افقی. فهرست کارها و تفکیک چهار باکس نیز باز شد؛ اطلاعات مهمانِ قبلی تغییر نکرد. تصویر دسکتاپ دستیار و تایپ فارسی بررسی شد.

## انتشار و مرز تحویل

- [CI 34697613659](https://github.com/tparkhondeh/personalAgent/actions/runs/34697613659) برای `be77c3a0d41895fadbf94938b01fa62a1312c5d6` موفق؛ ۳۲۰ تست، Type Check، Lint، Build و ۶۹ کنترل روی همان بسته Linux. بررسی امنیت وابستگی‌های runtime آسیب‌پذیری شناخته‌شده‌ای گزارش نکرد.
- Artifact `verified-web-34697613659`، ID `10299267568`؛ SHA ZIP روی سرور با GitHub برابر `8ce25d787516baa9cbc8137abf977133690f3c9ea1c6d1cd911707c123c711c7`. SHA بسته سرور `a7f67e83d2520a1f79b6975f7ef17f0ce30a5a69160ad3eb19d633637950fc66`؛ اندازه ۷۴٬۳۱۰٬۲۷۷ بایت، کپی محلی با همین هش محفوظ است.
- نصب فقط Staging با اسکریپت موجود و نسخه فعال مورد انتظار: Backup `/home/wealthos_dev/.staging/personal-agent/data/backups/pre-release-20260912T135533Z`، کد بازگشت `/home/wealthos_dev/.staging/personal-agent/releases/eb6193e8391c9049e04c1bc88aa2094b3945a7bf`. سلامت جدید ساعت `2026-09-12T13:56:52.468Z` تأیید شد. ۶۵ کنترل HTTPS موفق؛ شواهد خصوصی `/home/wealthos_dev/.staging/personal-agent/qa-final-KZTkja`.
- APK تازه لازم نشد: کد Android، mobile-shell، هویت نصب و امضا تغییر نکرده‌اند. digest فعلی Release ۴۰ با فایل عمومی موجود محلی برابر `909bf6f8b5dbb46f937210a787215006cfd7b223eaf880a359636fc1b2262fe9`، اندازه ۶۰٬۰۵۶٬۲۶۹ بایت؛ همان شواهد قبلی Android 13/14/16، نه اجرای دوباره آن‌ها. نسخه گوشی را حذف نکنید.
- آزمون اضافه Cache-Control روی URL عمومی **ناموفق** بود؛ Origin صحیح است ولی CDN برخلاف `no-store` کش چهار‌ساعته تحمیل می‌کند. body هر دو فایل با بسته برابر است، اما به‌روزرسانی قابل اعتماد هنوز نهایی نیست. خطای شبکه/TLS مرورگر این میزبان نیز جدا باقی است. [مانع، شواهد و اقدام پنل](STAGING_DELIVERY_BLOCKER_2026-09-12.md). هیچ CDN، DNS، TLS یا تنظیم Production تغییر نکرد.

LLM در لوکال و Staging بدون کلید و رضایت هزینه است؛ دستیار محلی فعال، تماس/پیامک واقعی خاموش، Push در Staging فقط configured است. موانع باقی‌مانده: پذیرش گوشی (صوت، ورود، Notification/Alarm/پس‌زمینه و اتصال)، کلید خصوصی و سقف هزینه ChatGPT، امضا/هویت پایدار و انتقال داده و تأیید Production.

Rollback این تغییر فقط بازگشت کد وب است، نه بازگردانی Snapshot قدیمی روی داده جدید. بازگشت به worker قبلی می‌تواند مشکل نمایش صفحه قدیمی را برگرداند؛ داده‌ها دست‌نخورده بمانند. Production، DNS، TLS، APK و مقصد آن تغییر نمی‌کنند.
