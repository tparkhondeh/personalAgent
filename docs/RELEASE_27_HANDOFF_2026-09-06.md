# تحویل نسخه آزمایشی ۲۷ — ۲۰۲۶-۰۹-۰۶

وضعیت: فایل آزمایشی ۲۷ منتشر و Staging هم‌نسخه شده است. تست‌های خودکار موفق‌اند؛ بازبینی تصویر، هشدار Pixel Launcher در شبیه‌ساز ۱۳/۱۴ را نشان داد. بنابراین تحویل برای آزمایش مالک است، نه تأیید نهایی همه کنترل‌های بصری.

## هویت فایل ساخته‌شده

- نام برنامه: `همراه پایدار 27`
- Package ID: `ir.wealthos.personalagent.stable27`
- Commit برنامه و Staging: `f3ad613f894b0d9032a5faf915ba3ded543db9fe`
- فایل: `Hamrah-stable-27.apk`
- نسخه: `1.0.27`؛ اندازه: `4,498,482` بایت.
- [دانلود مستقیم پایدار](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-27/Hamrah-stable-27.apk)؛ انتشار آزمایشی در ۲۰۲۶-۰۹-۰۶، ۰۸:۲۶:۳۰ UTC.
- SHA-256: `4489b31909afe90985b2ad9435fbdefbd137ea6fb5e291fa9163fe2615d15c9f`
- [اجرای کنترل کیفیت و انتشار](https://github.com/tparkhondeh/personalAgent/actions/runs/34021252124)
- منبع بسته: Artifact `9985606582`؛ فایل‌های `app.js`، `planner.js`، کنترل‌های تاریخ/ساعت، پالت و صفحه بازیابی داخل همین APK با منابع فعلی مقایسه و برابرند. فایل محرمانه‌ای در فهرست منابع بسته پیدا نشد.

## اصلاحات این نسخه

- سبک برنامه‌ریزی بی‌اثر و گزینه‌های آن از وب حذف شدند؛ رابط آفلاین از قبل این گزینه را نداشت. سوابق دیتابیس باقی هستند و ذخیره تنظیمات بدون آن آزموده شد.
- عنوان «تکرار برنامه» از پیگیری هشدار کار عقب‌افتاده تفکیک شد؛ تعداد/فاصله پیگیری فقط با روشن‌بودن تشدید هشدار نمایش داده می‌شوند.
- نام نمایشی کانال `NATIVE` برابر `Notification` شده؛ شناسه کانال، مجوز و رضایت لازم تغییر نکرده است.
- زمان‌ها، یادآوری‌ها، برنامه‌های موجود و پیش‌نویس مالک بدون تغییر باقی ماندند.

## شواهد وب و سرور

- ۱۷۵ تست، Type Check، Lint، Build وب، Audit وابستگی‌ها، Android Lint و چهار تست واحد بومی در Job ساخت موفق‌اند.
- ۲۷ کنترل جریان تأیید و هشت کنترل حفظ زمان‌بندی روی پایگاه ایزوله CI و دوباره روی Staging همین Commit موفق‌اند؛ داده‌های مصنوعی آزمون از حساب مالک جدا هستند.
- ورود/خروج حساب مصنوعی، ذخیره تنظیمات بدون سبک، سه یادآوری، استخراج عنوان و ساعت، فعال/غیرفعال‌شدن جزئیات پیگیری، نام Notification و انصراف در مرورگر واقعی Staging بررسی شدند.
- نمای موبایل ۳۹۰ پیکسلی بدون سرریز افقی بود؛ کنسول مرورگر خطای تازه نداشت. حساب آزمایشی خارج شد و هیچ پیشنهاد آزمایشی مرورگر اجرا نشد.
- [Staging هم‌نسخه](https://personalagent.wealthos.ir:8443/)؛ [لوکال](http://localhost:3001/).

## شواهد Android و محدودیت بازبینی بصری

- Build، هر سه Job مربوط به Android 13/14/16 و انتشار در Run بالا موفق‌اند. همان فایل یک‌بار ساخته شد و بدون بازسازی آزموده و منتشر شد؛ ۱۱ تست بومی در هر Android موفق‌اند.
- چهار گزارش تطبیق WebView در هر Android موفق‌اند: فونت فارسی، شعر و تاریخ، پالت، سه یادآوری بومی و لغو آن‌ها، تقویم شمسی، ساعت ۲۴ساعته، ثبت بدون ویرایش و عدم اجرای پیشنهاد پیش از تأیید.
- گزارش‌های سناریوهای قطع اینترنت، اجرای دوباره، DNS خراب، سرور خاموش و SSL نامعتبر موفق‌اند. جست‌وجوی Logcat سناریوها خطای FATAL EXCEPTION، Fatal signal یا Uncaught TypeError/ReferenceError/SyntaxError پیدا نکرد؛ این به معنی نبود همه خطاهای سیستم نیست.
- نه تصویر واقعی (رابط روشن، تاریک و بازیابی شبکه در سه Android) دستی دیده شد. در Android 16 رابط فارسی و صفحه بازیابی بدون صفحه سفید و بدون پنجره مزاحم دیده شدند.
- **استثنای مهم:** تصاویر Android 13 و 14 پنجره `Pixel Launcher isn't responding` روی رابط واقعی برنامه دارند. Logcat مرحله آماده‌سازی، ANR در `com.google.android.apps.nexuslauncher` را به‌ترتیب ساعت 08:19:08 و 08:18:44 UTC ثبت کرده است. این شاهد خطای Launcher شبیه‌ساز است، نه اثبات Crash همراه؛ با این حال تصاویر پوشیده‌شده را تأیید بصری کامل حساب نمی‌کنیم. کنترل CI فعلی این پنجره سیستمی را مانع انتشار نکرده؛ پیش از انتشار نهایی باید کنترل سطح سیستم و تصاویر بدون پوشش تکمیل شوند. هیچ تصویر/لاگی حذف یا دست‌کاری نشد.
- شواهد در `backups/local/delivery27-android33`، `delivery27-android34` و `delivery27-android36` نگه‌داری شدند (Artifactهای `9985747520`، `9985739987` و `9985742232`).
- فایل از لینک پایدار Release دوباره دانلود شد؛ SHA-256 آن، Asset منتشرشده و Artifact آزموده‌شده برابر است. نسخه بازدانلودشده در `backups/local/Hamrah-stable-27-release-verified.apk` حفظ شد.

## پشتیبان و بازگشت

- پشتیبان تاریخچه محلی: `C:/Users/pc/Desktop/project-backups/personal-agent-delivery27-20260906/source-history.bundle`؛ Git آن را معتبر تشخیص داد.
- Snapshot سالم داده محلی: `backups/local/hamrah-2026-09-06T08-15-38-115Z.db`.
- پیش از ارتقای Staging، کد قبلی، تنظیمات خصوصی، وضعیت سرویس‌ها و SQLite در `data/backups/pre-release-20260906T081853Z` حفظ شدند. Migration روی کپی کنترل شد.
- بازگشت کد به `releases/07f98c06e24b877b8934763a5f94942be76add4d` ممکن است؛ طبق راهنمای استقرار، ابتدا Scheduler آزمایشی متوقف شود و پایگاه قدیمی روی داده تازه بازنویسی نشود.
- Production، DNS، پورت ۴۴۳ و Proxy اصلی تغییر نکردند. صفحه اصلی قبل از استقرار HTTP 200 بود؛ مسیر `/api/health` روی Production قدیمی 404 است و نباید با Health محیط آزمایشی اشتباه شود.
- Health در ۰۸:۳۱:۵۲ UTC، Commit بالا و دیتابیس متصل را تأیید کرد؛ integrity دیتابیس سالم است. فضای آزاد پارتیشن اصلی سرور فقط `578200 KiB` (حدود ۵۶۵ MiB) است؛ برای استقرار بعدی مدیریت ظرفیت لازم است. هیچ فایل قدیمی برای آزادسازی فضا حذف نشد.

## مواردی که این تحویل ثابت نمی‌کند

- پذیرش همین فایل روی گوشی مالک، رفتار کیبورد/مجوزهای واقعی و تحویل Push واقعی هنوز معوق‌اند.
- شبکه GitHub ممکن است پورت ۸۴۴۳ را مسدود کند؛ آزمون WebView متصل روی گوشی با آزمون آفلاین و مرورگر Staging یکی نیست.
- سرویس واقعی ChatGPT، تبدیل صوت، تماس و پیامک بدون کلید/رضایت/هزینه مشخص فعال نشده‌اند.
- بسته آزمایشی شناسه جدا دارد؛ داده محلی نسخه قدیمی خودکار منتقل نمی‌شود. نسخه قبلی را برای حفظ آن داده‌ها حذف نکنید؛ داده حساب سرور با ورود به همان حساب قابل استفاده است.
