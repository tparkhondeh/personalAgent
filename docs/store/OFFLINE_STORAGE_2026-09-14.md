# تکمیل مستقل حفاظت حافظه آفلاین — ۱۴ سپتامبر ۲۰۲۶

## محدوده و نتیجه

این نوبت ابتدا با اجرای هم‌زمان «راه‌اندازی پروژه روی لوکال» برخورد کرد. تا پایان آن هیچ ویرایشی انجام نشد؛ سپس از `main` تمیز `3640927729c0ea445df58e77c87fd291c4851ff2` ادامه یافت. تغییرات `8bab4df` و مستندات CI حفظ شدند. این بخش رفع ایراد کد و آماده‌سازی است، نه تحویل نسخه عمومی یا تأیید اطلاعات واقعی.

در `mobile-shell/app.js` خواندن خراب با فهرست خالی اشتباه می‌شد؛ نوشتن ناموفق پس از تغییر وضعیت در حافظه نیز می‌توانست نمای نادرست نشان بدهد. کنترل مشترک محلی `storage.js` اکنون:

- شروع واقعاً خالی را از JSON خراب، ساختار ناسازگار و دسترسی ناممکن جدا می‌کند؛ داده قبلی را پاک/اصلاح خودکار نمی‌کند.
- قبل از هر نوشتن، بایت‌های خوانده‌شده را با آخرین وضعیت شناخته‌شده مقایسه می‌کند. تغییر تشخیص‌داده‌شده پنجره دیگر بازنویسی نمی‌شود؛ این جایگزین پایگاه داده تراکنشی چنددستگاهی نیست.
- ذخیره ناموفق را موفق اعلام نمی‌کند؛ فرم باز و رکورد قبلی در فهرست می‌ماند. اطلاعات انجام‌شده/شناسه هشدار و فیلدهای قدیمی معتبر حفظ می‌شوند.
- از ثبت دوباره هم‌زمان فرم جلوگیری می‌کند؛ ثبت شناسه اعلان باید قبل از زمان‌بندی بومی موفق شود. تأیید دستیار در شکست ذخیره برنامه، به وضعیت قابل‌بررسی برمی‌گردد.
- در صفحه آفلاین عادی و نسخه جاسازی‌شده بازیابی دقیقاً از همان کنترل استفاده می‌کند؛ API/حساب/رمز/دامنه/شناسه بسته تغییر نکردند.

محدودیت مهم: لغو اعلان قبلی و ذخیره داده دو عملیات مستقل‌اند. اگر لغو موفق ولی ذخیره بعدی ناموفق باشد، پیام خطای حافظه باقی می‌ماند و اعلان باید جدا بررسی شود. تراکنش بازیابی میان پل بومی و ذخیره‌سازی، export/import، همگام‌سازی با حساب و مهاجرت بسته/Origin هنوز تکمیل نشده‌اند. **این اصلاح به‌تنهایی مجوز اتکا به یادآوری یا تنها نسخه اطلاعات مهم نیست.**

## شواهد تازه

- پیش از تغییر: `backups/local/offline-storage-20260914T1010/before.bundle` با کل تاریخچه verify شد؛ archive کد SHA `8108764b1661e03688740b2a286521dabbf6d718789c727bb770569eb1f5c35a`.
- SQLite snapshot `backups/local/hamrah-2026-09-14T10-06-23-926Z.db`؛ بازیابی جدا `restore-drill-qaJfvJ/restored.db` در `10:10:23Z` با integrity/FK سالم و محتوای هر ۱۸ جدول بدون تغییر. حساب/کلید/سرویس جدید ایجاد نشد.
- ۴۷۴ تست در ۵۴ فایل، Type Check، Lint و Build مجزای وب موفق. ۲۰ آزمون جدید کنترل حافظه و جاسازی؛ آزمون اندازه تصویر بازار پس از تغییر مستقل دوباره موفق. audit تولید صفر آسیب‌پذیری شناخته‌شده؛ تضمین کلی امنیت نیست.
- مرورگر واقعی، فقط با حافظه جایگزین **ساختگی** در `http://127.0.0.1:3018`: شروع خالی با صفر نوشتن؛ ایجاد و تکمیل سالم؛ quota در ایجاد/تکمیل با صفر نوشتن موفق و حفظ رکورد؛ JSON خراب با هشدار و صفر بازنویسی. تصویر فرم خطا بصری بررسی شد. این ابزار هیچ localStorage واقعی مالک را نمی‌خواند و حساب/کلید ندارد. ماندگاری در بازخوانی کنترل‌شده در تست واحد بررسی شد؛ این برابر ارتقای گوشی نیست.
- Android lintRelease موفق و ۵ تست واحد بومی بدون تغییر همچنان up-to-date هستند. اجرای تازه شبیه‌ساز همان اصلاح در بخش پایین ثبت شد. `adb devices` هیچ گوشی متصل نشان نداد. Notification/Alarm/میکروفون فیزیکی و ارتقا روی گوشی آزموده‌نشده‌اند.
- APK تحویلی قبلی هنوز preview40 است: GitHub Release در `2026-09-09T23:17:20Z`؛ هش فایل محلی با digest عمومی `909bf6f8b5dbb46f937210a787215006cfd7b223eaf880a359636fc1b2262fe9` برابر است. فایل امضاشده عمومی جدید منتشر نشده است.
- `10:03:42Z`: health دامنه۴۴۳ پاسخ۲۰۰، بدون buildCommit؛ HTTPS۸۴۴۳ از ویندوز شکست handshake؛ worker عمومی max-age۱۴۴۰۰. `10:12:22Z`: Staging loopback و symlink دقیقاً `94adb4b`، DB سالم، worker Origin دارای no-store. هیچ Proxy/CDN/Production تغییر نکرد.
- تیکت۵۲۴۰۷ در مرورگر دوباره خوانده شد؛ آخرین پاسخ همان ۲۲ شهریور،۱۵:۱۲:۱۶: Page Rule پورت ندارد؛ پشتیبانی اختلال شبکه۸۴۴۳ را گزارش کرده است. این توضیح ارائه‌دهنده است، نه اثبات مستقل علت کل اتصال. پیام جدید ارسال نشد.
- `10:18:05Z`: ASR loopback با احراز هویت پاسخ۲۰۰/ready؛ هیچ صوتی ارسال نشد. LLM کلید ندارد و costApproved=false؛ Push تنظیم دارد ولی actualDelivery همچنان unverified.

## تأیید همان کد، CI و بسته‌های بدون امضا

- کد `56978d4d39e8f7e6866441650ec30e7282530d7a` روی `main` همین پروژه Push شد؛ `git ls-remote` تازه دقیقاً همان شناسه را تأیید کرد. شاخه‌های Gold/Silver در این نوبت موضوع کار نبودند و تغییر نکردند.
- [وب، Run34832681611](https://github.com/tparkhondeh/personalAgent/actions/runs/34832681611) موفق؛ شواهد دانلودشده در `artifacts/offline-storage-20260914/web-ci` شامل ۲۷ کنترل دستیار، ۱۱ رابط/نشست، ۲۳ تکمیل/حفظ سوابق، ۸ حفظ زمان‌بندی و ۲۸ کنترل امنیت/آمادگی است: مجموع ۹۷ کنترل موفق روی داده ساختگی جدا. سرویس خارجی فراخوانی نشد. بسته وب SHA `ffeefffeb00b5f62b5be28c078996baa019d17d775a4ae1b77f6a91d8ae2fba5` با manifest برابر است؛ Deploy نشد.
- [Android16، Run34832681637](https://github.com/tparkhondeh/personalAgent/actions/runs/34832681637) برای همان کد موفق؛ XML دانلودشده ۱۱ آزمون، صفر شکست/خطا/skipped را تأیید کرد. آزمون‌ها شامل اعلان و لغو آن، Alarm، پل بومی، میکروفون origin نامطمئن و رابط فارسی‌اند؛ **همگی روی شبیه‌ساز، نه گوشی مالک**. تصویر `artifacts/offline-storage-20260914/android-ci/artifacts/android/emulator.png` بصری بررسی شد: چهار شمارنده صفر و رابط فارسی بدون پوشش خطای سیستم. جست‌وجوی الگوهای crash مشخص در logcat صفر تطبیق داشت؛ بی‌خطایی مطلق ادعا نمی‌شود. Lint Debug صفر خطا و سه هشدار غیرمسدودکننده (دو نسخه وابستگی و یک متن بومی) داشت.
- APK داخلی CI با SHA `c6f530197dda8d81aa1bc84a9378f1dca995a58b8a1c960df62160e69b63a932` به سرور شبیه‌ساز `10.0.2.2:3001` متصل است؛ **فایل عمومی یا جایگزین APK40 نیست**.
- `bundleRelease` و `assembleRelease` محلی موفق شدند؛ بررسی ابزار امضا نشان داد هر دو خروجی عمداً **بدون امضا** هستند. هیچ کلید جدیدی ساخته نشد. بسته `ir.wealthos.personalagent`، code1/version1.0، min24/target36 و endpoint فعلی Staging۸۴۴۳ دارد؛ مقصد و هویت عمومی هنوز تصویب نشده‌اند. این خروجی‌ها آماده‌سازی‌اند، نه نسخه قابل نصب/ارسال نهایی یا مسیر ارتقای `.stable40`.
- AAB محلی `android/app/build/outputs/bundle/release/app-release.aab`، SHA `c475ca72c65bd33b24861c04287dce3a73a2aa3ae13b9d1571d31c783176fde5`؛ APK محلی `android/app/build/outputs/apk/release/app-release-unsigned.apk`، SHA `e41a76d5d50e16cd6cdc8d7be60996de1814b9d293e7f749f713f73ac0898cb2`. هر دو از درخت مبتنی بر `3640927` حاوی اصلاحات بعداً ثبت‌شده در `56978d4` ساخته شدند؛ بایت‌های `storage.js` و `app.js` بسته با کد آزموده برابر بود. شناسه Commit جاسازی‌شده نهایی ادعا نمی‌شود. در فهرست ZIP فایل env/database/keystore یا گواهی امضا نبود؛ بازبینی بسته امضاشده نهایی همچنان لازم است.

## فروشگاه و استفاده واقعی

منابع رسمی دوباره خوانده شدند: [ثبت‌نام Play،۲۵ دلار یک‌باره](https://support.google.com/googleplay/android-developer/answer/6112435?hl=en)، [۱۲ تستر/۱۴روز برای حساب شخصی مشمول](https://support.google.com/googleplay/android-developer/answer/14151465?hl=en)، [target36 از۳۱اوت۲۰۲۶](https://support.google.com/googleplay/android-developer/answer/11926878?hl=en)، [کشورهای مجاز](https://support.google.com/googleplay/android-developer/answer/9306917?hl=en)، [حذف حساب](https://support.google.com/googleplay/android-developer/answer/13327111?hl=en)، [مجوز Notification](https://developer.android.com/develop/ui/compose/notifications/notification-permission)، [اجازه Alarm دقیق](https://developer.android.com/about/versions/14/changes/schedule-exact-alarms).

[راهنمای رسمی بازار](https://developers.cafebazaar.ir/fa/guidelines/developers-panel/overview) در مرورگر خوانده شد: قرارداد و اطلاعات ناشر/اشتراک سالانه، APK یا AAB، و سرصفحه PNG با نسبت۵:۲/حداقل۷۲۰×۲۸۸/زیر۱MB. سرصفحه جدا در `artifacts/store-drafts/tia-8F4TT5/bazaar-header-720x288.png` ساخته و بصری بررسی شد (۱۲۳۵۰بایت، SHA `34aa20f0bc8e77817cc2e38c94fd7d2d40be57baa0961c9cba625662edb038b0`). آیکون و گرافیک Play قبلی بازاستفاده شدند. وضعیت حساب ناشر/مبلغ فعلی بازار بررسی نشده؛ هیچ قرارداد/ثبت‌نام/پرداخت/ارسالی انجام نشد.

هر چهار هدف هنوز **مانع دارد**: استفاده مطمئن شخصی، کاربران دیگر، Play و بازار. [جدول موانع و زمان مشروط](READINESS_2026-09-14.md) و [حدود داده/بازیابی/امضا](OPERATIONS.md) همچنان معتبرند. قدم بعد مهندسی حفاظت/انتقال داده و چرخه حساب است؛ محیط واقعی به انتخاب مقصد نهایی، کلید مجاز و سیاست نگهداری/حذف مالک وابسته است. هنوز همه آماده‌سازی‌های فنی پایان نیافته‌اند؛ پذیرش گوشی/فروشگاه هم مستقل است.
