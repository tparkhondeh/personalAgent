# آمادگی استفاده واقعی و فروشگاه‌ها — ۱۴ سپتامبر ۲۰۲۶

## نتیجه فعلی

به‌روزرسانی همان روز: [حفاظت حافظه آفلاین و شواهد تازه کد `56978d4`](OFFLINE_STORAGE_2026-09-14.md) شامل ۴۷۴ تست، ۹۷ کنترل HTTP، ۱۱ آزمون شبیه‌ساز، CI موفق و ساخت AAB/APK بدون امضاست. اعداد و مراحل قبلی پایین، سابقه ارزیابی نخست هستند؛ وضعیت چهار مانع زیر تغییر نکرده و هیچ نسخه جدیدی روی گوشی یا سرور منتشر نشده است.

| هدف | نتیجه | علت |
|---|---|---|
| تنها محل نگهداری اطلاعات مهم مالک | مانع دارد | نسخه نهایی روی مسیر پایدار، حفظ/انتقال اطلاعات گوشی، بازیابی و پذیرش گوشی کامل نیست |
| کاربران دیگر | مانع دارد | علاوه بر موارد بالا، بازیابی/حذف حساب، سیاست حریم خصوصی عملی، پشتیبانی و عملیات چندکاربره باقی است |
| Google Play | مانع دارد | حساب قانونی/آزمون مشروط، هویت امضا و بسته عمومی، حذف حساب و اظهارات داده نهایی نشده‌اند |
| کافه‌بازار | مانع دارد | حساب/قرارداد/اشتراک، حریم خصوصی داخل برنامه، نسخه عمومی امضاشده و بررسی فروشگاه باقی است |

ورود داده آزمایشی ممکن است؛ فعلاً برنامه را تنها محل نگهداری اطلاعات مهم ندانید. این نتیجه ارزیابی است، نه تأیید امنیت یا انتشار. بخشی از موانع **کار برنامه‌نویسی باقی‌مانده** است، نه فقط انتظار برای مالک یا تیکت.

## شواهد نسخه‌ها و شبکه

- شروع از `main` تمیز `58b1b43b385f97dd017f70870f678dd1378fe5cc`، همسان با GitHub. آخرین کد اصلاح اعلان بومی `94adb4b` و تغییر آزمون داخلی `b070867` بود.
- لوکال ۳۰۰۱: health برابر ۲۰۰/DB سالم؛ نسخه توسعه فاقد buildCommit عمومی است. مشاهده واقعی عنوان tia، وزیرمتن، چهار باکس و صفحه دستیار با شمارنده‌های قبلی حفظ‌شده انجام شد. تصویر دسکتاپ ۱۳۶۶×۹۰۰ و موبایل ۳۹۰×۸۴۴ بازبینی شد؛ عرض افقی اضافه مشاهده نشد. تصاویر گذرای تغییر اندازه مدرک نهایی نیستند.
- Staging: symlink و health در `2026-09-14T09:19:30Z` روی `94adb4b3ba02764283ee088a95e52c55738ab1b3`؛ پردازش و دیتابیس سالم. عنوان/فایل‌های JS/CSS آن tia هستند.
- **۴۴۳ اکنون health برابر ۲۰۰ دارد، نه ۴۰۴ گزارش دیروز.** عنوان و مجموعه فایل‌های صفحه با سرویس قدیمی Production روی ۳۰۱۱ برابرند، نه Staging. PM2 مسیر release `49cceed` را نشان داد؛ تاریخ آن Commit برابر `2026-09-01T07:44:49-07:00` است. عامل/زمان تغییر شبکه احراز نشد؛ این بررسی Production را تغییر نداده است.
- ۸۴۴۳ از سرور با TLS عادی پاسخ ۲۰۰ داد؛ از ویندوز handshake شکست خورد. علت قطعی را به DNS نسبت نمی‌دهیم. `sw.js` عمومی روی هر دو پورت `max-age=14400` دارد؛ پاسخ ۲۰۰ اثبات سیاست کش صحیح نیست. Origin کد جدید برای worker، no-store دارد؛ تنظیم مؤثر CDN باید محدود و جدا بررسی/اصلاح شود.
- تیکت ۵۲۴۰۷ بعد از بارگذاری مجدد: آخرین پاسخ قابل مشاهده همچنان ۱۴۰۵/۶/۲۲ ساعت ۱۵:۱۲:۱۶ است. Page Rule شرط پورت ندارد؛ پورت Origin با پورت آدرس کاربر فرق دارد. تغییر hostname مشترک می‌تواند Production را جابه‌جا کند. پاسخی ارسال/تنظیمی تغییر داده نشد.
- own-server ASR در ۳۰۲۰ با احراز هویت سلامت `ready:true` دارد؛ فقط health آزموده شد، نه ارسال صدای مالک. Staging کلید OpenAI ندارد و رضایت هزینه false است. پیکربندی Web Push وجود دارد؛ دریافت واقعی Notification/Alarm/Push روی گوشی مالک هنوز مدرک تازه ندارد. تماس/SMS اختیاری فعال نشده‌اند.

## حفظ وضعیت و نسخه تمیز

- پشتیبان خارج ریپو: `C:/Users/pc/Desktop/project-backups/tia-store-readiness-20260914`، bundle کل تاریخچه verified و archive کد SHA `9939f8ccabedd9d9f6fad5cb8099ca126e4dc717b7ea92ed6a410aa358b8ca27`؛ فایل‌های قواعد/وضعیت نیز کپی و هش وضعیت برابر شد. شروع بدون تغییر ذخیره‌نشده بود.
- snapshot سازگار SQLite: `backups/local/hamrah-2026-09-14T09-19-42-341Z.db`؛ SHA `4bb78ce7ce2dcbe50097dc516284b6aebfdf4d51a39750d9c4a6efd6ba0dba24`. Restore جدا `restore-drill-dWDSGf/restored.db` با integrity/FK و حفظ محتوای هر ۱۸ جدول موفق بود؛ هیچ scheduler یا سرویس خارجی اجرا نشد.
- محیط تازه: `backups/clean-start/tia-DfBKQW/tia.db`، زمان `2026-09-14T09:25:51.026Z`، ۱۷ جدول کاری خالی، صفر حساب/کلید؛ SHA `f738127e092a5bdccf62967c0ca68ec12ea51dfff51f18a65aad9379ce36d748`. فقط آماده‌سازی ذخیره‌سازی است، **نه حساب/سرور آماده استفاده**. [روش فعال‌سازی مجاز و بازیابی](OPERATIONS.md).
- ایراد واقعی وب: `demoItems` در شروع مهمان بود؛ حذف شد، بدون حذف موارد ذخیره‌شده قبلی. خواندن ناموفق حافظه دیگر به نوشتن خودکار فهرست خالی منجر نمی‌شود. نوشتن فقط در اقدام کاربر و پیش از تغییر نمای موفق انجام می‌شود؛ خرابی/عدم دسترسی/پرشدن حافظه پیام واضح می‌دهد و داده قدیمی را حفظ می‌کند. در خطای خواندن، همگام‌سازی هشدار مهمان نیز متوقف است تا فهرست خالی جای داده نامعلوم استفاده نشود.
- این تغییر **در کد/لوکال است، نه Staging/Production**. رابط آفلاین APK40 از ابتدا با فهرست خالی شروع می‌کند ولی مسیر خطای حافظه و export/import آن به بازبینی/اصلاح و نسخه آزموده تازه نیاز دارد. اطلاعات قبلی مهمان لوکال (چهار مورد) حفظ شدند؛ عمداً پاک نشدند.
- بکاپ خودکار Production بررسی شد: `17 2 * * *` در زمان سرور +03:30، ۱۵ فایل، آخرین `hamrah-20260913T224701Z.db`، integrity/FK سالم. اسکریپت موجود حدود ۳۰ روز نگه می‌دارد و قدیمی‌ترها را حذف می‌کند؛ این اسکریپت اجرا/ویرایش نشد. بکاپ‌ها روی همان سرور و رمزگذاری‌نشده‌اند؛ بکاپ امن خارج میزبان هنوز تأیید نشده است.

## APK و امضا

آخرین GitHub Release بررسی‌شده همان `phone-preview-stable-40`، انتشار `2026-09-09T23:17:20Z` است. نام **tia آزمایشی 40**، Package `ir.wealthos.personalagent.stable40`، نسخه `1.0.40`/code40، source `34180286b90008498c839f3ec3b4fd72749c750a`، SHA `909bf6f8b5dbb46f937210a787215006cfd7b223eaf880a359636fc1b2262fe9`، اندازه ۶۰٬۰۵۶٬۲۶۹ بایت.

فایل محلی تحویلی دوباره هش شد؛ `aapt` نام/شناسه و target36/min24 را تأیید کرد. فهرست ZIP هیچ `.so` ندارد؛ `zipalign -c -P 16 4` موفق بود. این‌ها جای آزمون بسته نهایی عمومی/دستگاه 16KB نیستند. مجوزهای واقعی در [فهرست داده‌ها](DATA_SAFETY_DRAFT.md) ثبت شدند. APK آزمایشی را نسخه قابل ارسال فروشگاه حساب نکنید؛ کلید امضای عمومی ساخته نشده است.

شواهد Android13/14/16 **همان APK40** از [Run34414607065](https://github.com/tparkhondeh/personalAgent/actions/runs/34414607065) موجود است؛ این بار کل ماتریس بدون تغییر باینری تکرار نشده. آزمون داخلی Android16 Run34768185035 برای کد بومی اعلان قبلی است، نه پذیرش گوشی مالک یا فایل جدید فروشگاه. برای تغییر مستقل وب/اسکریپت‌ها APK تازه بی‌دلیل منتشر نشد.

## الزامات رسمی بررسی‌شده

### Google Play

- [ثبت‌نام](https://support.google.com/googleplay/android-developer/answer/6112435?hl=en): حساب و احراز هویت با مشخصات قانونی؛ هزینه ثبت اولیه ۲۵ دلار است. حساب/مدرک/پرداختی ایجاد یا ارسال نشد.
- [کشورهای ثبت توسعه‌دهنده](https://support.google.com/googleplay/android-developer/answer/9306917?hl=en): ایران در فهرست فعلی نیست. کشور/نوع حساب قانونی مالک هنوز مشخص نیست؛ از زبان/شماره او نتیجه‌گیری درباره اقامت نکنید و راه دورزدن پیشنهاد ندهید.
- [آزمون حساب شخصی جدید](https://support.google.com/googleplay/android-developer/answer/14151465?hl=en): برای حساب شخصی ساخته‌شده بعد از ۱۳ نوامبر ۲۰۲۳، حداقل ۱۲ آزمایش‌کننده برای ۱۴ روز پیوسته پیش از درخواست دسترسی Production لازم است. پایان دوره به معنی پذیرش قطعی نیست؛ وضعیت حساب مالک نامعلوم است.
- [Target API](https://support.google.com/googleplay/android-developer/answer/11926878?hl=en): صفحه فعلی برای درخواست‌های جدید/آپدیت از ۳۱ اوت ۲۰۲۶ API36 را تعیین می‌کند؛ کد و APK40 هدف ۳۶ دارند، اما این یک معیار از چند معیار است.
- [حذف حساب](https://support.google.com/googleplay/android-developer/answer/13327111?hl=en): وقتی در برنامه حساب ساخته می‌شود، مسیر درون برنامه و راه اینترنتی درخواست حذف لازم‌اند. این مسیرها هنوز ساخته/آزموده نشده‌اند.
- [Data safety](https://support.google.com/googleplay/android-developer/answer/10787469?hl=en)، [مجوزهای حساس](https://support.google.com/googleplay/android-developer/answer/16558241?hl=en)، [امضای برنامه](https://developer.android.com/studio/publish/app-signing)، [سازگاری اندازه صفحه](https://developer.android.com/guide/practices/page-sizes) باید بر اساس بسته و سرویس نهایی تکمیل شوند. SCHEDULE_EXACT_ALARM با اجازه کاربر با USE_EXACT_ALARM محدودشده یکی نیست؛ مجوز جدید اضافه نشد.

### کافه‌بازار

- [راهنمای رسمی پیشخان](https://developers.cafebazaar.ir/fa/guidelines/developers-panel/overview#Study-the-contract) در مرورگر واقعی خوانده شد: قرارداد، اطلاعات ناشر و اشتراک سالانه؛ APK یا App Bundle پذیرفته می‌شود. **مبلغ فعلی اشتراک، هویت/حساب مالک و زمان بررسی در پنل احراز نشده‌اند**؛ قیمت حدسی ارائه نمی‌شود. هیچ قرارداد/ثبت‌نامی پذیرفته نشد.
- [حریم خصوصی رسمی](https://developers.cafebazaar.ir/fa/app-publish-guidelines/rules/privacy): متن در برنامه و قبل از جمع‌آوری اطلاعات باید در دسترس باشد؛ کاربرد داده و اشخاص ثالث شفاف، مجوزها در زمان نیاز و رضایت پیش از استفاده غیرمنتظره لازم است. صفحه در دسترس تاریخ آخرین به‌روزرسانی ۱۴۰۱ دارد؛ مبلغ یا الزام جدید پنل را از این تاریخ استنتاج نکنید.
- [متن معرفی و تصاویر پیش‌نویس](LISTING.fa.md)، [پیش‌نویس سیاست](PRIVACY_DRAFT.fa.md)، [فهرست داده و مجوزها](DATA_SAFETY_DRAFT.md) آماده‌اند، نه تأیید حقوقی/فروشگاهی. هویت مسئول، تماس پشتیبانی، نگهداری/حذف و مدل عرضه نیازمند تصمیم مالک هستند.

## آزمون و موارد باقی‌مانده

Type Check، Lint، ۴۵۴ آزمون/۵۳ فایل و Build مجزای وب موفق شدند؛ پس از سخت‌گیری نوع داده، ۱۱ آزمون هدفمند و Lint دوباره موفق بودند. Android lintRelease با صفر خطا/یک هشدار و ۵ آزمون release unit موفق شد؛ امضای APK40 صریحاً Android Debug و fingerprint آن `4ca0dbc79a8db200028c7ff21951a6bae40b225604e29bc42bb39632a510e95c` بود. audit وابستگی‌های تولید صفر آسیب‌پذیری گزارش‌شده داشت؛ این بررسی تضمین امنیت نیست.

مرورگر واقعی، نگهداری چهار مورد قدیمی، داشبورد و تایپ دستیار در موبایل/دسکتاپ را نشان داد؛ متن ساختگی فقط تایپ و سپس پاک شد، ارسال/ساخت حساب انجام نشد. شروع خالی/خرابی حافظه با تست کنترل‌شده اثبات شد، نه با پاک‌کردن حافظه مرورگر مالک. آزمایش جانبی origin متفاوت `127.0.0.1:3001` در رابط روی Loading ماند، با وجود GET نشست برابر۲۰۰/null؛ این آزمون موفق حساب نشد و آدرس تحویل نیست. مسیر اصلی localhost سالم بود. هیچ تنظیم امنیت/Origin برای عبور از آن تغییر نکرد. CI تازه بعد از Push جدا ثبت می‌شود.

پیشنهاد آیکون ۵۱۲ و گرافیک ۱۰۲۴×۵۰۰ از نشان موجود tia ساخته و بصری بررسی شد؛ در `artifacts/store-drafts/tia-IuonfJ` با manifest هش قرار دارد. دو تصویر واقعی APK40 بدون داده شخصی مالک فقط به‌عنوان پیش‌نویس حفظ شدند، نه تصاویر نسخه نهایی. مجوز نصب‌شده web-push برابر MPL-2.0 بود؛ اشتباه MIT در مستند قدیمی اصلاح شد و بازبینی NOTICE نسخه نهایی باقی است.

تأیید بعد از Push کد `8bab4df48d5f8ac72c85a0ad7e5dc0d75ec0afea`: [CI34830214606](https://github.com/tparkhondeh/personalAgent/actions/runs/34830214606) و آزمون همان بسته وب روی DB جدا موفق؛ [Android34830214587](https://github.com/tparkhondeh/personalAgent/actions/runs/34830214587) با ۱۱ آزمون/صفر خطا/صفر skipped موفق. Artifact شواهد `10341333982` در `artifacts/store-readiness-20260914-ci` نگه‌داری شد؛ تصویر `android/artifacts/android/emulator.png` نمایش واقعی فارسی و چهار شمارنده صفرِ شروع خالی را بدون پوشش خطای سیستم نشان داد. این APK Debug داخلی برای آزمون ساخته شد و جایگزین Release40 یا آزمون ارتقای گوشی مالک نیست. CI تازه سرور را Deploy نکرد.

Logcat اجرای داخلی هیچ تطبیقی برای FATAL EXCEPTION، E AndroidRuntime یا Uncaught TypeError/ReferenceError نداشت؛ **بی‌خطایی مطلق لاگ ادعا نمی‌شود**. WebView اخطار شناخته‌نشدن payment/usb در Permissions-Policy و دو پیام caught با مضمون `Error injecting safe area CSS` داشت. نمایش فارسی/نوار پایین و ۱۱ آزمون موفق بودند؛ بازبینی safe-area روی گوشی و بسته عمومی در پذیرش نهایی باقی است. برای حذف اخطار، سیاست امنیتی ضعیف نشد. SHA فایل داخلی آزمون `b771da5202a3fce6995b4beae1181f8f307262230398d3b01362737d42f3fbea` است؛ آن را با SHA فایل تحویلی۴۰ یکی نگیرید.

| مانع | اقدام لازم | مسئول | زمان مشروط، نه تعهد |
|---|---|---|---|
| مسیر پایدار و نسخه صحیح/کش | تصمیم محدود برای origin، پشتیبان، تأیید انتقال و smoke HTTPS | مدیر سرور + مالک برای تأیید؛ عامل برای آزمون | حدود نیم تا یک روز کاری پس از دسترسی/تصمیم مسیر |
| داده گوشی و بازیابی | export/import، مدیریت خطای حافظه آفلاین، آزمون ارتقا و تعارض | عامل توسعه؛ پذیرش نهایی مالک با گوشی | حدود ۱–۳ روز کاری توسعه/آزمون؛ وابسته به مسیر نسخه نهایی |
| کاربران عمومی | بازیابی/حذف حساب و نشانی‌های حریم خصوصی/حذف | عامل توسعه + مالک برای هویت/سیاست/روش ارسال | حدود ۲–۴ روز کاری پس از تصمیم‌ها؛ برآورد همپوشان |
| عملیات پایدار | مقصد/کلید مجاز بکاپ رمزدار، پایش و Restore جدا | مالک/مدیر سرور + عامل | حدود نیم تا یک روز پس از تأمین دسترسی |
| گوشی، صوت و هشدار واقعی | چک‌لیست یک‌باره Notification/Alarm/پس‌زمینه/صوت/ارتقا | مالک با فایل آزموده‌شده | حدود ۳۰–۶۰ دقیقه؛ زمان رفع ایراد احتمالی جدا |
| Google Play | حساب قانونی، امضا/AAB، فرم‌ها و آزمون مشمول | مالک + عامل؛ بررسی نهایی فروشگاه | اگر حساب مشمول باشد حداقل ۱۴ روز پیوسته آزمون، سپس زمان بررسی نامعلوم |
| بازار | حساب/اشتراک/قرارداد و نسخه امضاشده/فرم‌ها | مالک + عامل؛ فروشگاه | زمان احراز و بررسی از پنل/فروشگاه تعیین می‌شود |

اتصال واقعی LLM هنوز کلید، رضایت هزینه و آزمون می‌خواهد؛ نسخه محلی را ChatGPT فعال معرفی نکنید. تماس و پیامک اختیاری‌اند. بدون تصمیم دامنه/هویت انتشار و آزمون واقعی، امضا یا انتشار عمومی انجام نمی‌شود.

لینک‌های بررسی‌شده: [لوکال](http://localhost:3001/)، [Staging با محدودیت اتصال](https://personalagent.wealthos.ir:8443/)، [Production قدیمی، نه نسخه نهایی](https://personalagent.wealthos.ir/)، [APK40 فقط آزمایشی](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-40/tia-stable-40.apk).
