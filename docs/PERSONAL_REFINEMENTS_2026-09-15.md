# Personal-use refinements — 2026-09-15

Scope: the owner's seven requested refinements. No Production/CDN/domain, operational key, paid provider, account credential, phone package, or installed data change. Source, local UI, server deployment, tested binaries and phone acceptance remain separate gates.

## Preservation and starting point

- Started on clean `main`, local and fresh remote `6d56db7c84f477f19299cb01884383ed646c803c` (last code `95bbda432b096416385585aa8492b8aa2b8f5d1f`).
- Recoverable history: `C:/Users/pc/Desktop/project-backups/tia-refinements-20260915/main.bundle`, verified with `git bundle verify`. HEAD source archive in the same folder, SHA-256 `e0dcdd1e5b90eb0687b1cdc0b59df1f9e01381883ef62cd079ab4323745697ef`.
- Consistent SQLite snapshot `backups/local/hamrah-2026-09-15T09-54-11-122Z.db`, SHA-256 `6254ea3c6afbfa908d6d7b76af5b246ed36ca93a61790b3f3333d3ce4dff4029`. Restore drill `backups/local/restore-drill-LjsnfB/verification.json`: integrity/FK OK; all 18 existing tables identical after migrations on a separate copy; no live data/scheduler touched.

## Behavior

1. Manual save emits `done` **only after persistence**, closes the form, selects Today/all. This is not status DONE. A guest message explicitly says device-only. Server failure keeps the form and idempotency key. Storage errors never overwrite unreadable data or falsely announce success.
2. Poem tracks use measured content width and shared `minmax(0,...)` fractions, baseline alignment, a compact mobile gap, and one shared 13–18px size at the normal 16px root. This secondary-text floor scales with text enlargement. Oversized words may break only when wrapping is necessary; no characters are removed. The corpus is unchanged.
3. Login resolves the actual Better Auth session with cookie-cache bypass before rendering. A valid session redirects only to the allowlisted existing destination. Existing 30-day remembered lifetime / transient unchecked cookie / logout / revocation / expiry are unchanged. No password is stored in app storage; independent OS/password-manager biometrics are not bypassed.
4. Personal poems are four validated lines in a shared web/offline editor. `tia.personal-poem.v1:<scope>:<Tehran day>` is **device-local, not server sync**. Signed-in users, guests and days have separate namespaces; no private poem enters the Rumi collection. Reset/next deactivates the daily override but retains its text for editing. Corruption, quota, unavailable storage and concurrent changes fail closed. A dialog opened before midnight cannot save onto the new day. No claim of Rumi authorship for custom text.
5. Three original bundled alarm sounds, native per-device persistence and Android system volume settings; notification/alarm audio usages remain distinct. See [sound implementation](ALARM_SOUNDS_2026-09-15.md). Existing scheduled channels/times must remain unchanged when selecting a sound.
6. Zero repeats means an initial alert and no follow-up, not zero alerts. Task recurrence remains separate. New server-created immediate Android attempts have a single persisted 10-second transport allowance; it is never renewed on fetch/retry. Guest overdue chains anchor to the recorded deadline, not each render's current time. Expired native schedules are not rebased into a burst. Ordinary positive-repeat policies retain their earlier semantics. Offline new manual records snapshot only an explicitly saved repeat setting; existing records and approved drafts retain their values.
7. Emergency name/phone and optional SMS/call controls live under a collapsed optional section. Normal Alarm/Notification needs neither. Previously stored values remain in submitted form data; entering a phone number is explicitly not verification. Delivery consent/provider gates remain intact.

## Evidence recorded so far

- Actual browser on local synthetic account: remembered login redirects without showing credentials again; manual save from tia returns to Today with one open (not completed) task; completion removes it from active lists but keeps count. Zero repeats saves and survives reload; interval disappears; optional contact fields stay hidden.
- Personal-poem browser test: four entered synthetic lines render in two rows, survive reload, cancel preserves prior text, next returns to the unchanged Rumi collection. Guest text was not shown after switching to the synthetic account.
- 360-poem real-browser QA: initial 390px run had 107 fully single-line quatrains. Updated 390px/16px root: 267 single-line, 93 readable wraps, no lost/clipped words. A 320px/32px-root run exposed an overlong word; wrap fallback fixed it, then all 360 passed without missing words. Reports stay in ignored `artifacts/speech-poems/`. Desktop 1280px initial run: 360 single-line; final rerun tracked below.
- Focused sessions/manual-save suites: 105 tests/8 files passed. Repeat-zero suites: 153/9 passed. These overlap and must not be added together as a total.
- Final regenerated combined suite: **644/644 tests passed** (`artifacts/refinements/unit-results.json`); Type Check, Lint and isolated production web Build passed. Production dependency audit: no known vulnerabilities. Source-level guard expectations were updated to the new persisted ID/time snapshot, without removing storage-failure behavior tests.
- Independent review found owner-wide clear queued behind a stalled network load. Clear now invalidates immediately and cancels existing owned alarms without waiting for the load. An in-flight native write cancels its exact IDs in `finally`, including lost replies; future syncs retain a cancellation barrier. Five additional regression checks cover stalled load, permission resolution, partial native failure, the urgent adapter and dashboard account cleanup/late-JSON guard. Invalidated operations acknowledge no retained IDs. Both approved reminders and urgent alarms are cleared at account boundaries.
- Local Android `lintDebug`, `testDebugUnitTest`, `assembleDebug`, `assembleDebugAndroidTest` all passed; lint has five warnings and no errors. The bundled speech model was verified from the built APK (53,389,181 bytes; approved model hash unchanged). This internal debug build is not an APK40 upgrade.
- Final desktop 1280px/16px root: **360/360 single-line**; 390px: 267 single-line / 93 readable wraps; 320px at doubled root font: all 360 readable without clipped/missing words. Browser geometry checks also verify paired baselines and column alignment.
- Actual bundled-interface browser at 390x844: audio preview resolved after explicit click; saved sound and zero-repeat selection survived reload. Manual save showed `done — فقط روی این دستگاه ذخیره شد.` and Today, then the still-open task was found in Tasks after reload; synthetic completion removed only its active row. Personal-poem save/reload rendered the same four custom lines, aligned in two rows, without Rumi attribution. Browser media preview is not audible native alarm acceptance.
- Android 13/14/16 instrumentation and exact artifact/CI evidence are recorded separately below once available; no publication or safe upgrade claim before those gates.

## Remote verification and isolated Staging

- Source commit `7130c91ac69c33e024ed23525cf0af12bf98bc5c` pushed to main. [Web CI 34960497745](https://github.com/tparkhondeh/personalAgent/actions/runs/34960497745) passed, including **163 HTTP checks** against the packaged server and a separate synthetic database; approval, retries, completed-item visibility, preserved schedules, authorization and cache policy pass. No paid provider was invoked by those checks.
- Verified web archive SHA-256 `507d97f1e260a4c9221ebc4c63ab3f6429115bbbfdd5bf8fd0985bad7de2023e` matched on download and server. Deploy script backed up SQLite/config/process metadata, integrity-checked the database and migrated a separate copy before switching **only Staging3010**. Backup: `/home/wealthos_dev/.staging/personal-agent/data/backups/pre-release-20260915T105957Z`. Retained rollback release: `95bbda432b096416385585aa8492b8aa2b8f5d1f`. No new schema changes in this refinement.
- 11:00Z health on origin3010 and public8443 both identified `7130c91`; 30 read-only cache/hashed-asset checks on origin3010 passed. Production symlink remained `/home/wealthos_dev/apps/personal-agent/releases/49cceed`. Rollback retains current data; stop only the Staging scheduler before reverting code, never restore an old database over new writes.
- Public browser remains a distinct limitation: the 8443 page eventually rendered the older single focus card, **without the new + or four cards**, despite fresh origin health. Public `sw.js` still received `cache-control: max-age=14400` from CDN. No cache/worker/cookie deletion, CDN rule change or Production mutation was performed. Local3001 and the connected emulator visibly render the new UI. Thus public health alone is not end-user acceptance or proof that the stale-display issue is fixed.
- [Connected Android16 CI 34960497786](https://github.com/tparkhondeh/personalAgent/actions/runs/34960497786) passed **12 instrumented tests**, zero failed/skipped, including the actual alarm-sound bridge/channel/persistence test. Its Persian screenshot was visually reviewed: four cards, two-row poem and +, no system-error overlay. This internal emulator-endpoint APK is not the owner update.
- [Three-Android run 34960504077](https://github.com/tparkhondeh/personalAgent/actions/runs/34960504077) builds one internal candidate for all three emulators with publication disabled. Candidate metadata: `tia آزمایشی 41`, package `ir.wealthos.personalagent.stable41`, source `7130c91`, SHA-256 `55fff6dbd94793af273fa8820055dbd3f1db610428d06b604ad21fc748c15c55`. It is **not published, not an APK40 update and must not be installed as a data-preserving replacement**. Final matrix/image evidence follows separately.

### Rejected internal candidate41 / manual-next-form regression

All three offline matrix jobs rejected candidate41. Android34/36 logs located the failure in `dashboardUiQa`'s consecutive manual creation loop, after the first persisted task: closing the form preceded the end of native scheduling, so its shared submit button remained disabled and the next form's submit was ignored. No timeout/assertion was weakened. A new unit test first reproduced the disabled button (25 prior tests pass, new case fails), then the handler was corrected: release the button once persistence completes, reject duplicate submits from a closed modal, and use per-form generations so late reminder results/finally blocks cannot overwrite a new form's feedback/state. Editing tests explicitly reopen the form. The final combined suite is 645 tests; candidate41 remains rejected and unpublished. A new APK is required for this code fix, rather than relabeling the rejected bytes.

The Windows same-build compressed-model check is not a cross-platform archive check: tar/gzip metadata differ from Linux. The existing `verify-released-speech-model.py` verified candidate41 against the pinned upstream ZIP: all 18 decoded files and the patched engine match, APK SHA matches metadata. Keep both gates: same-build exact bytes in CI, decoded pinned-source verification after cross-host download. No speech weights were changed in this refinement.

### Final source and second isolated deployment

- Source `289a76fbc30f9a0c18519fab0cc2cfd9ec690ccb` fixes the reproduced next-form race. [Web CI 34962242066](https://github.com/tparkhondeh/personalAgent/actions/runs/34962242066) passed: **645/645 unit tests**, Type Check, Lint, production Build and all **163 HTTP checks**. [Connected Android16 CI 34962242163](https://github.com/tparkhondeh/personalAgent/actions/runs/34962242163) also passed. Existing tests were not relaxed.
- Final packaged web SHA-256 `3eaa1cab83d4f5c2f1313b0576a4e174030a25bc8b3c7632eb244b69cb7692fe` matched locally and on the server. Isolated Staging3010 was switched using the same safe deployment script, after backup `/home/wealthos_dev/.staging/personal-agent/data/backups/pre-release-20260915T112040Z`; rollback code is `7130c91ac69c33e024ed23525cf0af12bf98bc5c`. At 11:21:37Z origin health identified `289a76f`, the database was connected and 30 read-only origin cache checks passed. Production remained `49cceed`. No schema change, CDN policy change or user-data rollback was performed.
- The public-browser stale-display limitation above remains unresolved. Only the origin is verified updated. Final local3001 browser visibly displays the four cards, poem + and current interface; synthetic account testing ended logged out, without clearing its records or the browser's storage.
- Internal candidate42 from [run 34962247040](https://github.com/tparkhondeh/personalAgent/actions/runs/34962247040): name `tia آزمایشی 42`, package `ir.wealthos.personalagent.stable42`, source `289a76fbc30f9a0c18519fab0cc2cfd9ec690ccb`, SHA-256 `ffdf106158a12f3f7997a41c62046ef3f391e0ce905ff3b50153aa444332015f`. Download hash matches metadata; the pinned speech-model verifier confirms all 18 decoded files and engine unchanged. **Publication is disabled; this is an internal test binary, not a compatible APK40 upgrade.**
- Candidate42 Android13 passed all UI/failure scenarios and 12 native tests. Persian screenshots of the dashboard, dark settings and the real open keyboard were visually inspected: no system-error overlay, visible save button, preserved text. The 360-poem checks allow readable wrapping; the dashboard screenshot's long poem wraps naturally without dropping words.
- Candidate42 Android14 also passed all scenarios and 12 native tests, including cold relaunch, offline, DNS/server-down/invalid-SSL recovery, manual CRUD, completed counts only, three reminders and cancellation. Dashboard/dark-settings/open-keyboard/SSL-recovery screenshots were visually reviewed without system-error overlays. Android13/14 each measured all 360 poems in the real 412px WebView: 332 single-line, 28 readable wraps, no missing words. All 11 Android13 log-verification reports were clear with no classified observation exceptions; successful Android14 checks are retained separately in `artifacts/refinements/android-final-34/`.
- Candidate42 Android16 passed the consecutive-creation/dashboard checks and 12 native tests, then was rejected by the unchanged log gate at 11:28:28Z: `FATAL EXCEPTION: UiAutomation`, PID6708, `Bad file descriptor`. The same PID is explicitly the UID2000 shell UIAutomator, not the application. Fresh system-UI evidence was clear; nevertheless this incomplete run is **not** accepted. [Exact-binary Android16 retest 34963797373](https://github.com/tparkhondeh/personalAgent/actions/runs/34963797373) was dispatched without rebuilding, changing assertions or enabling publication. All original logs remain in ignored artifacts. Remaining matrix/retest outcomes are recorded below when observed.

### Android16 observation-tool diagnosis

The first exact-binary retest `34963797373` again passed the UI/native checks through invalid-SSL recovery, but stopped at a second shell capture failure (11:43:11Z, PID7647). Both complete logs show UID2000, `com.android.commands.uiautomator.Launcher`, a completed `AccessibilityNodeInfoDumper` fetch and the same Binder/accessibility-cache callback exception; neither identifies the tia process. Fresh XML still identifies `ir.wealthos.personalagent.stable42` with no error overlay. The previously accepted shell exception rule required an optional `Shutting down VM` line; these traces do not contain that line for the failing PID. Repeated unmodified retries were stopped.

The observer classifier now accepts this additional **fully evidenced shell-only variant** using the completed dump before the exception plus the exact BinderProxy/AccessibilityCache stack. It still requires the same PID/UID/launcher, exact exception, no application Process identity and fresh unobscured UI. Missing evidence, wrong tag/PID/UID, a later dump, other exceptions, renderer/JavaScript/unsafe SSL failures remain hard failures. A synthetic regression first failed and then passed, including every negative control; no raw private logs are committed. Existing capture/visual assertions and the actual APK are unchanged. Both original failures remain recorded; retrospective classification is **not** a pass for unexecuted scenarios. A fresh full Android16 exact-binary run is required after the QA-only change.

The AOSP [DumpCommand implementation](https://android.googlesource.com/platform/frameworks/uiautomator/%2B/e056d9d08eddc0d64ac637b2f07cb0e4d6693c32%5E1..e056d9d08eddc0d64ac637b2f07cb0e4d6693c32/) shows dump followed by automation disconnection. This supports the observer-lifecycle distinction; attribution of these two incidents is based on their actual PID/UID/stack and XML evidence, not an assertion that all Android16 errors are harmless.

Final QA-only revalidation: Type Check and Lint passed; all **646/646 tests in 67 files** passed in a serial run (`artifacts/refinements/unit-results-serial.json`). The initial concurrent run left seven login cases unexecuted during fixture setup; it was not counted as success. The unchanged login suite then passed 20/20 independently and in the full serial run, without raising timeouts. The production web Build/163 HTTP checks and APK bytes remain those already verified at `289a76f`; no runtime source was changed by the observer fix.

### Accepted internal exact-binary matrix

[Final Android16 retest 34965992619](https://github.com/tparkhondeh/personalAgent/actions/runs/34965992619), with observer commit `f6a0022f77f81456c5950933ecc2c0fa093ad8ff`, passed every original scenario and 12 native tests. The unchanged candidate42 was verified against source run34962247040 before installation. All 11 final log-verification reports are clear, with **zero application failures and zero classified observer exceptions in this final run**. Dashboard, dark settings, real keyboard and SSL-recovery screenshots were visually reviewed: no system-error overlay or blank WebView. All 360 poems were checked at 412px: 332 single-line, 28 readable wraps, no missing text. Raw evidence stays in `artifacts/refinements/android-accepted-36/`.

Together with the successful Android13/14 jobs in34962247040, the **same candidate42 bytes have now passed Android13/14/16**. Android14 retained one previously supported shell-teardown observation; it is not an app crash and was not deleted. The two rejected Android16 executions and rejected candidate41 remain documented. Web/connected CI, local tests and these emulator results do not prove phone audio delivery, compatible installation or public Staging cache freshness.

**No new GitHub Release was published.** The explicit owner gate requires a verified data-preserving compatible upgrade before phone delivery; that gate is unresolved. Candidate42 metadata above is for internal traceability only. APK40 remains the last published release and the existing phone app must be retained.

## Owner upgrade boundary

The unchanged published APK40 was freshly hashed: `909bf6f8b5dbb46f937210a787215006cfd7b223eaf880a359636fc1b2262fe9`, package `ir.wealthos.personalagent.stable40`, code `34180286b90008498c839f3ec3b4fd72749c750a`.
Its signer SHA-256 is `4ca0dbc79a8db200028c7ff21951a6bae40b225604e29bc42bb39632a510e95c`; the available local debug signer is `4719b4f61ca80d09e9f94859f9a954c0a8570bb8fee233dec23a30260b77cc47`. They are incompatible for in-place upgrade. A focused backup-directory search found no additional `.keystore`; this is not proof that every possible key copy is lost. No phone is currently attached.

An internal emulator APK is not a replacement for APK40 and must not be presented as a safe owner upgrade. Do not uninstall the old app, change its identity or clear Web Storage. Await the owner's installed-version/data-location confirmation and either the original signing material through a secure path or a separately tested preservation/transfer plan. Store release is out of scope. Existing Staging/cache/phone-acceptance limitations are not fixed by these local changes.
