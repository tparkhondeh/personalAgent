# تحویل صوت، تأیید امن و پالت مشترک — ۲۰۲۶-۰۹-۰۶ UTC

## مرجع نسخه

- مبنای شروع این نوبت: `d2d6c679983e8d7a0feae2148118b6d901692547`؛ نسخه منتشرشده ۱۹ و Staging در `17afb3d` بودند. تغییرات تازه در این نوبت ساخته شدند، نه بازیابی از یک چت قدیمی.
- کد تحویلی: `158ab5422a1a38307f28fe6014ba1cc78bde9164` روی `main`، Push شده است. Commit پس از آن فقط مستندات تحویل است؛ کد APK، وب و Staging یکسان است.
- [اجرای موفق نسخه ۲۲](https://github.com/tparkhondeh/personalAgent/actions/runs/34013442504): ساخت، هر سه Android و انتشار موفق‌اند.
- [دانلود مستقیم پایدار APK](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-22/Hamrah-stable-22.apk)؛ [صفحه Release و تصاویر](https://github.com/tparkhondeh/personalAgent/releases/tag/phone-preview-stable-22).
- نام دقیق: **همراه پایدار 22**؛ Package ID: `ir.wealthos.personalagent.stable22`؛ Version: `1.0.22`، Code: `22`.
- SHA-256: `772812cebb39f0708123716f761d5248ae2e1e4c903646f08289cbba174f28fb`؛ اندازه: ۴٬۴۸۸٬۸۷۷ بایت؛ انتشار: `2026-09-06T05:23:38Z`.
- APK دوباره از لینک دائمی دانلود شد؛ هش فایل محلی، Artifact آزموده‌شده و Digest دارایی GitHub برابرند. هفت فایل رابط داخلی با منبع فعلی تطبیق شدند؛ فایل محیطی یا Keystore داخل APK نیست.
- [لوکال همین دستگاه](http://localhost:3001) و [محیط آزمایشی HTTPS](https://personalagent.wealthos.ir:8443) فعال‌اند؛ Health در `2026-09-06T05:24:13Z` کد `158ab54` و اتصال دیتابیس را تأیید کرد. لوکال از پوشه اصلی اجرا می‌شود.
- نسخه ۲۲ کنار نسخه قبلی نصب می‌شود؛ **نسخه قبلی را حذف نکنید**. داده محلی Package قبلی خودکار منتقل نمی‌شود؛ داده حساب از همان Staging پس از ورود خوانده می‌شود. این انتشار آزمایشی است، نه نسخه عمومی Production.

## تغییرهای واقعی

| بخش | نتیجه و مرز کارکرد |
|---|---|
| پالت | منبع مشترک CSS وب؛ گرادیان، رنگ‌ها و فونت داخلی در Android. تیره‌سازی خودکار WebView غیرفعال است؛ حالت روشن/تاریک با CSS کنترل می‌شود. |
| فهم فارسی | عنوان، زمان نسبی و شمسی، صبح/عصر، مدت نامشخص، سه یادآوری، اصلاح ادامه گفتگو و انتخاب مورد موجود. ساعت مبهم سؤال می‌خواهد. تست محدود است، نه تضمین فهم تمام جمله‌ها. |
| تأیید | پیش‌نویس سروری متعلق به حساب، شماره نسخه، انقضا، ویرایش، انصراف، اجرای تکرارناپذیر و ثبت اتمی. Alarm به اجازه و همگام‌سازی دستگاه وابسته است. |
| تکرار | روزانه/هفتگی با ۲ تا ۱۲ نوبت مشخص؛ هر نوبت رکورد جداست. ویرایش گروهی سری پشتیبانی نمی‌شود. |
| صوت | ضبط کوتاه، توقف/لغو، رضایت ارسال همین صوت، تبدیل به متن قابل‌ویرایش، سپس پیشنهاد و تأیید جدا. شنود پس‌زمینه یا آرشیو صوت خام ندارد. |
| OpenAI واقعی | فعال نیست: کلید و تأیید هزینه موجود نیست. آزمون مصنوعی قرارداد API جای آزمون زنده را نمی‌گیرد. |
| آفلاین | موتور فارسی و پیش‌نمایش داخل APK هستند؛ داده‌ها محلی و مستقل از حساب‌اند. تشخیص صوت، Push و مرکز اعلان حساب در حالت آفلاین فعال نیستند؛ اعلان بومی و Alarm با مجوز دستگاه کار می‌کنند. |
| تماس و پیامک | سرویس واقعی خاموش؛ هیچ تماس، پیامک یا هزینه واقعی ایجاد نشده است. |

## شواهد این نوبت

- Type Check، Lint بدون خطا/هشدار، ۱۱۷ تست و Build مستقل وب موفق شدند.
- ۲۵ بررسی واقعی HTTP با حساب‌های مصنوعی مجزا روی لوکال موفق: قبل از تأیید هیچ کار/جلسه‌ای ایجاد نشد؛ حساب دیگر، نسخه قدیمی و تأیید بدون رضایت رد شدند؛ تکرار تأیید رکورد تکراری نساخت؛ سه یادآوری و لغو Alarm پس از تکمیل آزموده شدند.
- Android Lint: بدون issue؛ چهار تست واحد و ساخت Debug/Test APK محلی موفق‌اند. گزارش‌های FlatDir و اختلاف نسخه ابزار SDK، هشدار ابزارند و آزمون بومی واقعی را جایگزین نمی‌کنند.
- همان APK تحویلی روی Android 13/14/16 نصب و Cold Launch شد؛ ۱۱ تست بومی در هر سه موفق‌اند. متن فارسی، RTL، فونت داخلی، شعر و دو تاریخ، سه اعلان و لغو آن‌ها، پالت روشن/تاریک، اصلاح پیشنهاد، نبود اثر قبل از تأیید و حفظ کانال هشدار تأییدشده موفق‌اند.
- تصاویر روشن، تاریک و بازیابی هر سه نسخه بصری بررسی شدند. قطع اینترنت، اجرای دوباره و بازگشت به رابط محلی با APK اصلی آزموده شدند. خطای DNS، سرور خاموش و SSL با APK تشخیصی هم‌کد و مقصد خرابِ عمدی بررسی شدند؛ این سه APK تشخیصی فایل تحویلی نیستند.
- شبکه Runner به پورت ۸۴۴۳ دسترسی نداشت؛ موفقیت CI اثبات اتصال آنلاین Android به Staging نیست. سلامت آنلاین و مسیر ثبت/تکمیل در مرورگر واقعی جداگانه بررسی شدند. آزمون اتصال و میکروفون در WebView گوشی مالک باقی است.
- Logcatها Crash برنامه یا خطای JavaScript مهارنشده نشان ندادند. پیام مهارشده `Error injecting safe area CSS` از `SystemBars` خود Capacitor هنگام تعویض سند دیده شد؛ بلوک try/catch آن در منبع وابستگی بررسی شد. صفحه نهایی، متن و محل دکمه‌ها سالم‌اند؛ پیام را حذف یا پنهان نکرده‌ایم و گزارش «بدون هرگونه خطا» نمی‌دهیم. در پذیرش گوشی حاشیه نوار سیستم و کیبورد نیز بررسی شود.
- شواهد کامل محلی در `backups/local/android22-33`، `android22-34` و `android22-36`؛ Artifact ساخت در `backups/local/stable22-build` و دانلود دوباره در `backups/local/released-stable22.apk` محفوظ‌اند. تصاویر در Release دائمی‌اند؛ Artifact کامل GitHub نگهداری ۱۴روزه دارد.
- وب در ۳۹۰×۸۴۴ و ۱۲۸۰×۹۰۰ بررسی بصری شد. تب همراه، تقویم، متن فارسی، شعر و دو تاریخ سالم بودند. پالت مرورگر وب و رابط داخلی برابر بود. پیش‌نویس مصنوعی بدون ثبت کار لغو شد؛ تنظیمات و داده حساب مالک تغییر نکردند.
- کاندید ۲۰ به‌علت مرگ وابسته به سرویس فونت گوگل در Android 14 منتشر نشد؛ مشکل با حذف فقط Initializer فونت آنلاین اصلاح شد، نه با حذف آزمون یا نادیده‌گرفتن شکست. کنترل جلوگیری از بازگشت این وابستگی اضافه شد.
- کاندید ۲۱ نیز در Android 14 یک رویداد بومی زودتر از آماده‌شدن پل JavaScript اجرا کرد؛ خطا باعث توقف انتشار شد. اصلاح محدود، نسخه‌دار و با Hash ثابت در `patches/` نگه‌داری می‌شود؛ آزمون بومیِ پیش از آماده‌شدن و پس از آماده‌شدن اضافه شد. Android 13/16 کاندید ۲۱ موفق بودند، اما نسخه ۲۱ منتشر نشد.
- Staging در `0ed931f` با ۲۵ کنترل HTTP و Smoke موفق آزموده شد. در مرورگر واقعی، جلسه نمونه با ساعت ۱۷ و سه یادآوری پیشنهاد شد؛ مدت ۴۵ دقیقه با ویرایش و پیش‌نمایش جدید اضافه و فقط پس از تأیید ثبت شد؛ سپس تکمیل تأییدشده انجام شد. API مستقل وضعیت DONE و صفر Alarm منتظر دستگاه را تأیید کرد.
- Staging نهایی `158ab54` نیز ۲۵ کنترل HTTP را گذراند؛ پس از ارتقا، مرورگر واقعی صفحه فارسی، وزیرمتن، پالت و خروج از حساب QA بدون داده خصوصی را تأیید کرد. Worker یادآوری و Escalation در `05:26:04Z` پاسخ موفق داشتند.
- یک تکرار اضافی آزمون محلی به Rate Limit پنج ثبت‌نام در ساعت رسید؛ محافظ حذف یا دور زده نشد. همین ۲۵ کنترل روی دیتابیس جداگانه CI و روی Staging موفق بودند. Audit وابستگی‌های Production نیز آسیب‌پذیری شناخته‌شده گزارش نکرد.
- ثبت و توقف واقعی میکروفون روی گوشی مالک، دقت فارسی OpenAI، زنجیره کامل صوت واقعی تا ثبت و تحویل Push واقعی هنوز اثبات نشده‌اند.

## پشتیبان و بازگشت

- کد و Git: `C:/Users/pc/Desktop/project-backups/personal-agent-voice-20260906-0420/source-and-history-without-live-db.tar`؛ داده مستقل با Integrity Check موفق: `backups/local/hamrah-2026-09-06T03-54-38-348Z.db`.
- Migration جدید فقط جدول/ستون اضافه می‌کند. پیش از Staging، Snapshot سالم داده، فایل محیطی و وضعیت سرویس‌ها گرفته می‌شود و Migration ابتدا روی کپی اجرا می‌شود.
- ارتقای اولیه این نوبت: Backup سرور `pre-release-20260906T050302Z` و کد `0ed931f`، نسخه بازگشت `17afb3d`. قالب پایان خط Windows در Helper پیش از اجرا اصلاح شد؛ اولین تلاش قبل از هر تغییر سرویس متوقف شده بود. `.gitattributes` پایان خط Script و Hash حساس Patch را بین Windows/Linux ثابت نگه می‌دارد.
- بازگشت فقط با سوییچ کد و Restart سرویس Staging است؛ Scheduler آزمایشی باید متوقف شود، چون کد قبلی سیاست تأیید هر مورد را نمی‌شناسد. Snapshot قدیمی روی داده تازه Restore نمی‌شود.
- ارتقای نهایی Staging: Backup سالم `/home/wealthos_dev/.staging/personal-agent/data/backups/pre-release-20260906T052335Z`؛ نسخه بازگشت `/home/wealthos_dev/.staging/personal-agent/releases/0ed931fc5ff9d181682f4648ff22c3ec860eb2fe`. Migration ابتدا روی کپی آزموده شد؛ Snapshot داده، تنظیمات خصوصی و وضعیت سرویس‌ها محفوظ‌اند.
- بسته سرور همان Build: SHA-256 `5277129653a3c843075fcc1691866c440fc0dc9563116172f12a6b36ff0151bf`. توقف کوتاه فقط هنگام Restart Staging رخ داد و Health پس از راه‌اندازی موفق شد.
- Production، DNS و Proxy اصلی دست‌نخورده‌اند: Production همچنان `49cceed` است؛ صفحه عمومی ۲۰۰ و `/api/health` عمومی همان ۴۰۴ قبلی است. فضای آزاد سرور در پایان حدود ۱٫۰۵ GiB است؛ هیچ فایل یا تاریخچه‌ای برای خالی‌کردن فضا حذف نشد. برای انتشار عمومی ظرفیت دیسک یک ریسک واقعی است.

## فعال‌سازی باقی‌مانده

1. کلید معتبر OpenAI فقط در تنظیمات خصوصی سرور، با تأیید مالک درباره هزینه. کلید در چت، Git یا APK قرار نگیرد.
2. حد مصرف داخلی تعداد درخواست است، نه سقف تضمینی دلاری. پیش از فعال‌سازی، وضعیت اعتبار و کنترل هزینه حساب بررسی شود؛ محدودیت کشور یا احراز هویت دور زده نشود.
3. آزمون زنده ابتدا با متن/صوت مصنوعی کم‌حجم و سپس آزمون اختیاری گوشی مالک انجام شود. ضبط صوت اجازه ساخت کار نیست؛ تأیید کارت همچنان لازم است.
4. انتشار عمومی تنها بعد از پذیرش گوشی، بررسی ظرفیت سرور، Backup و تأیید نهایی Production انجام شود.

### ترتیب گام بعدی و زمان مشروط

| اولویت | اقدام | برآورد، نه وعده |
|---|---|---|
| ۱ | نصب نسخه ۲۲ کنار قبلی و بررسی رابط/تب‌ها، مجوز میکروفون و اعلان روی گوشی مالک | حدود ۱۵–۳۰ دقیقه پس از در دسترس بودن گوشی |
| ۲ | تنظیم امن کلید و تأیید هزینه، یک آزمون مصنوعی واقعی متن و صوت | حدود ۳۰–۶۰ دقیقه پس از دسترسی معتبر؛ مشکلات حساب/پرداخت خارج از این برآوردند |
| ۳ | مسیر زنده وویس تا متن، ویرایش، تأیید و هشدار واقعی | حدود ۳۰–۶۰ دقیقه پس از دو مرحله قبل؛ بدون آن نهایی محسوب نمی‌شود |
| ۴ | ظرفیت سرور و آمادگی انتشار عمومی، سپس درخواست تأیید Production | وابسته به تأمین ظرفیت و تصمیم مالک؛ زمان قطعی فعلاً نداریم |

جزئیات فنی و حریم خصوصی: [راهنمای صوت و تأیید](VOICE_AND_APPROVAL.md). قواعد مصرف بهینه توکن در `AGENTS.md` و `DEVELOPMENT_RULES.md` ثبت شده‌اند.
