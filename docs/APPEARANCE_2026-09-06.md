# ظاهر یکسان و جمع‌وجور — ۲۰۲۶-۰۹-۰۶

## دامنه و حفاظت

پالت روشن لوکال مرجع است. حالت سیستم دیگر انتخاب تم نیست. انتخاب صریح روشن/تیره روی همان دستگاه حفظ می‌شود؛ این گزینه حساب، کارها یا زمان‌بندی را تغییر نمی‌دهد. Production در این مرحله خارج از دامنه است.

پیش از اصلاح: Git bundle کامل و تأییدشده در `C:/Users/pc/Desktop/project-backups/personal-agent-theme-20260906-164123/source-history.bundle` و Snapshot سالم SQLite در `backups/local/hamrah-2026-09-06T13-12-02-625Z.db`. HEAD آغاز `35a14e49abdb9f009226e4cdfa6feee9ea460c75` و درخت پاک بود.

## معماری

- `src/lib/appearance.ts`: اسکریپت مشترک پیش از نمایش؛ فقط light/dark معتبر، کلید جداگانه، تحمل نبود دسترسی ذخیره‌سازی.
- `src/app/globals.css`: مرجع رنگ؛ تولید خودکار `mobile-shell/theme.css` و رنگ‌های بومی Android از همین منبع. انتخابگرهای صریح و تخت CSS با WebView قدیمی سازگارند؛ وابستگی خودکار به prefers-color-scheme حذف شد.
- Android پیش از اولین `loadUrl` تم را در سند تزریق می‌کند. پیام تغییر فقط از main frame دو origin معتبر و فقط با مقدار light/dark پذیرفته می‌شود؛ SharedPreferences انتخاب را میان صفحه متصل و محلی یکسان نگه می‌دارد. صفحه بازیابی نیز مقدار بومی را هم‌زمان دریافت می‌کند.
- Android 12+ از per-app night mode برای هماهنگی Splash استفاده می‌کند؛ اجزای Loading و نوارهای سیستم هم تنظیم می‌شوند. WebView فاقد Document Start از قابلیت‌های موجود/بازیابی محلی استفاده می‌کند؛ نتیجه روی نسخه‌های هدف باید آزموده شود، نه فرض.
- چهار مصراع دو ردیف و دو ستون RTL هستند؛ متن بلند در همان ستون می‌شکند، کوچک‌سازی افراطی یا برش نداریم. فاصله‌ها، کارت‌های امروز، تنظیمات و همراه کوتاه‌تر شدند؛ فرم تأیید و قراردادهای امنیتی تغییر نکردند.
- cache پوسته PWA نسخه جدید دارد؛ داده‌های کاربر پاک نمی‌شوند.

## شواهد تا این نقطه

- ۲۱۸ تست در ۳۱ فایل؛ Type Check، Lint و Build محلی موفق شدند. پس از اصلاحات تکمیلی و پیش از تحویل مجدداً اجرا می‌شوند.
- مرورگر واقعی: تم روشن/تیره، حفظ انتخاب در تب تازه، نبود خطای JS؛ عرض‌های ۱۲۸۰، ۳۹۰ و ۳۲۰، شعر دو ستونه و نبود اسکرول افقی بررسی شدند.
- رفع دو خطای ساخت در همین مرحله: تعریف تکراری منبع Splash حذف شد (تنها فایل تازه اضافی این مرحله؛ نه داده قبلی)، وابستگی مستقیم AndroidX WebKit به app افزوده شد.
- ساخت محلی Android، چهار تست واحد و Android Lint بدون خطا یا هشدار موفق شدند؛ شرایط قابلیت WebView صریح کنترل شد. ۳۵ کنترل HTTP محلیِ تأیید/تکرار/یادآوری و ثبت بدون ویرایش در مرورگر موبایل موفق؛ یک جلسه فقط در حساب مصنوعی ثبت شد و داده‌های قبلی تغییر نکردند.
- APK منتشرشده هنوز ۲۸ است؛ تغییر کد را معادل انتشار APK تازه نمی‌گیریم. نتیجه Android 13/14/16 و لینک فایل تازه پس از آزمون ثبت می‌شود.

## بازگشت

قبل از Staging پشتیبان تازه لازم است. بازگشت کد با release قبلی و حفظ دیتابیس جاری انجام می‌شود؛ نه restore کور داده قدیمی. هیچ reset یا بازنویسی تاریخچه مجاز نیست. ترجیح ظاهر کلید جدا دارد و بازگشت کد داده کارها را حذف نمی‌کند.

## کاندید ۲۹ و استقرار آزمایشی

- Commit `988a8490d18f57c48826ef3b8676b6a198e02c3e`؛ [اجرای آزمون](https://github.com/tparkhondeh/personalAgent/actions/runs/34036947135). Build شامل ۲۱۸ تست و ۳۵ کنترل HTTP موفق شد؛ در زمان این ثبت آزمون سه Android هنوز جاری است، پس انتشار تأیید نشده است.
- هویت داخل فایل با aapt و SHA بررسی شد: `همراه پایدار 29`، `ir.wealthos.personalagent.stable29`، `1.0.29`، ۴۵۱۱۹۸۳ بایت، SHA-256 `a256a9e743e5e64d68a1203f1cb6326472e70a9c95d63da11e749e3679bcd09e`.
- Staging در ۱۳:۵۰ UTC به همین Commit ارتقا یافت. Backup سالم: `data/backups/pre-release-20260906T134959Z`؛ release بازگشت `f363336070cf1d84968b95013c07cbb52e5bbf83`. آرشیوهای قبلی و دیتابیس حفظ شدند. سلامت HTTPS و Commit پاسخ API و ۳۵ کنترل HTTP تازه موفق‌اند.
- مرورگر واقعی Staging: رنگ روشن، انتخاب تیره، شعر دو ستونه، CSS ساخته‌شده، تنظیمات و تقویم در دسکتاپ/موبایل سالم؛ خطای JS دیده نشد. قبولِ تم گوشی واقعی با پذیرش مالک جدا است.
- دریافت مستقیم Artifact از ویندوز به میزبان blob متصل نشد؛ همان فایل از اتصال SSH موجود دریافت و SHA بررسی شد. این اشکال دانلود زیرساخت، با خطای برنامه یکی نیست.
- Workflow جدید پیش‌فرض فقط Build و شواهد می‌سازد. انتشار خودکار یک ورودی اختیاریِ پیش‌فرض خاموش دارد؛ تا بررسی تصاویر، فایل عمومی نمی‌شود و مشکل قبلی 403 با افزایش مجوز حل نشده است. انتشار از اتصال موجود و مجاز مالک پس از بررسی انجام می‌شود.
- تلاش اول: Android 14 و 16 موفق؛ Android 13 پیش از نصب در boot سیستم متوقف شد. XML پیام `System UI isn't responding` و Logcat ساعت ۱۳:۵۱:۰۶ UTC، `ANR in com.android.systemui` را ثبت کردند. کنترل fail-closed درست عمل کرد؛ APK در آن runner هنوز نصب نشده بود. شواهد اصلی حفظ شدند.
- برای پرهیز از بازسازی APK یا تکرار بی‌دلیل دو آزمون موفق، Workflow آزمون Artifact موجود اکنون انتخاب صریح یک API را دارد؛ انتشار خودکار در این حالت ممنوع است. نتیجه Android 13 جدید باید با SHA همان Build ترکیب و جداگانه گزارش شود، نه اینکه اجرای شکست‌خورده موفق نامیده شود.

## منابع فنی

[WebViewCompat](https://developer.android.com/reference/androidx/webkit/WebViewCompat)، [Appearance و Splash](https://developer.android.com/develop/ui/views/theming/darktheme). سیاست مصرف بهینه توکن بدون حذف آزمون ضروری در AGENTS.md ثبت شده است.
