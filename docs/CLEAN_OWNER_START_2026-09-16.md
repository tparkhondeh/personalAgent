# شروع تمیز حساب مالک — ۱۶ سپتامبر ۲۰۲۶

مالک صریحاً تأیید کرد اطلاعات موجود آزمایشی‌اند و اجازه پاک‌سازی داد. این مجوز مربوط به داده آزمایشی tia است؛ نه حذف حساب ورود، کلید، سوابق هزینه، تاریخچه Git، پروژه‌های دیگر یا انتقال Production. الزام نگهداری انجام‌شده‌ها در آمار برای استفاده عادی همچنان برقرار است؛ این پاک‌سازی یک‌باره است.

## نتیجه واقعی

| محدوده | داده آزمایشی حذف‌شده | بررسی نهایی |
|---|---|---|
| حساب تأییدشده لوکال |۲۲ پیام،۱۱ گفت‌وگو،۶ پیشنهاد؛ کار/جلسه از قبل صفر بود | همه محتوای حساب، برنامه‌ها، اعلان‌ها و آمار صفر |
| همان ایمیل تأییدشده روی Staging |۲ کار،۲ جلسه،۲ رویداد تقویم،۹ یادآوری،۵ تلاش هشدار،۸ اعلان،۱۰ پیام،۴ گفت‌وگو،۲ پیشنهاد،۱ تنظیمات آزمایشی و۸ رویداد آزمایشی | شمارش همه محتوای هدف صفر؛ سلامت سرور و نسخه `2d4484c` تأیید شد |
| مرورگر لوکال |۵ برنامه مهمان شامل۴ فعال و۱ انجام‌شده، پیش‌نویس‌ها و داده شعر شخصی در محدوده tia | بازیابی۵ برنامه از پشتیبان واقعاً آزموده شد؛ سپس دوباره پاک و پس از Reload، امروز/کارها و همه شمارنده‌ها صفر شدند |
| گوشی | دسترسی مستقیم وجود ندارد؛ حافظه داخلی و Alarm قبلی روی گوشی پاک نشده‌اند | پاک‌سازی سرور را معادل پاک‌شدن گوشی یا لغو Alarm از قبل نصب‌شده ندانید |

حساب‌ها، رمز و نشست ورود، اشتراک Push، رکوردهای استفاده API، رسیدهای جلوگیری از ثبت تکراری و تمام ردیف‌های سایر حساب‌ها بدون تغییر حفظ شدند. کلید و دفتر بودجه مستقل اصلاً در عملیات پاک‌سازی وارد نشدند؛ بررسی تازه، سقف۲دلار و مصرف منظورشده۰٫۱۵۶۲۳۷دلار را بدون تغییر تأیید کرد. تماس خارجی/آزمون پولی جدید انجام نشد.

## پشتیبان و ایمنی عملیات

- لوکال: `C:/Users/pc/Desktop/project-backups/tia-clean-reset-20260916/local-snapshot-cVNmOR/before.db`؛ SHA256: `46778746e8192b394365aaeb7ae056b0218db99d055d4735419415a5679ba8e9`.
- Staging: `/home/wealthos_dev/.staging/personal-agent/clean-reset-20260916/staging-snapshot-K2nGXR/before.db`؛ SHA256: `dc150f46c939d66ef7785fdf350f559ae9f27c32b46a04ae755cf2ba293043dc`.
- هر Snapshot با روش سازگار SQLite گرفته شد، در نسخه جدا با تطبیق بایت‌ها بازیابی شد و سلامت/روابط بررسی شدند. پاک‌سازی ابتدا روی همان کپی انجام و حفظ دقیق همه ردیف‌های خارج از محدوده تأیید شد؛ سپس تراکنش زنده فقط با تطبیق وضعیت لحظه‌ای با Snapshot اجرا شد. تغییر هم‌زمان یا اثر ناخواسته باعث Rollback کامل می‌شود.
- سه آزمون مستقل ابزار نگهداری: حفظ سایر حساب‌ها/ورود/مصرف/رسیدها، رد مالک یا Snapshot نامعتبر، و Rollback اثر جانبی Trigger موفق‌اند. کد ابزار و گزارش خصوصی کنار پشتیبان، خارج از Git نگهداری می‌شوند.
- پشتیبان مرورگر در همان Origin با پیشوند `tia.clean-backup.20260916.` حفظ و خواندن مجدد و بازیابی آن تأیید شد. این پشتیبان خارج از دستگاه نیست؛ حافظه سایت را پاک نکنید. صفحه نگهداری فقط لوکال بود؛ پس از کار از مسیر قابل‌سرو حذف شد و نسخه همسانش در پوشه خصوصی پشتیبان محفوظ است. بازگردانی آزمایش‌شده، جایگزین بی‌اجازه داده‌های تازه نیست.

## مرز تحویل

[لوکال خالی](http://localhost:3001/) آماده مشاهده است؛ برای ذخیره حسابی از همان ورود موجود استفاده شود. داده تازه نباید به‌عنوان آزمایشی فرض و دوباره پاک شود. محتوا در APK بسته‌بندی نشده بود؛ پاک‌سازی داده سرور/مرورگر نیاز به Build یا APK جدید ندارد. کد برنامه تغییر نکرده و شواهد آزمون وب و Android ثبت‌شده در [گزارش تحویل](PERSONAL_HANDOFF_2026-09-16.md) معتبرند؛ داده پاک‌شده را با اجرای مجدد QA روی حساب مالک پر نکنید.

نگرانی حفظ اطلاعات آزمایشی قدیمی با رضایت مالک رفع شد، ولی اتصال عمومی پایدار، انتشار محدود Production، اجرای GPT روی سرور واحد، امضای پایدار نسخه روزمره و پذیرش واقعی گوشی همچنان جدا هستند. این عملیات نه انتشار نهایی گوشی است، نه مجوز تغییر دامنه. نسخه گوشی/Production تغییر نکردند و APK40 جدید یا نهایی معرفی نمی‌شود.
