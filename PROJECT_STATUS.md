# مختصات پروژه Personal Agent

آخرین به‌روزرسانی: ۲۰۲۶-۰۸-۳۱

این فایل نقشه ثابت پیشرفت پروژه است و بعد از هر مرحله مهم به‌روزرسانی می‌شود. درصدها تخمینی‌اند و بر اساس قابلیت‌های واقعاً پیاده‌سازی و آزمایش‌شده محاسبه می‌شوند.

## موقعیت فعلی

- پیشرفت MVP: حدود **۸۶٪**
- مرحله فعلی: فعال‌سازی سرویس LLM و آزمایش زنده Agent
- آخرین مرحله کامل: اتصال زمینه واقعی کاربر و حافظه گفتگو به Agent
- قدم بعدی: ساخت کلید OpenAI توسط مالک حساب، قرار دادن امن آن در محیط محلی و اجرای آزمایش زنده
- انتقال دامنه: عمداً تا پایان پروژه متوقف است

محیط لوکال و دیتابیس موجود بدون حذف داده‌ها آماده شده‌اند. به‌دلیل اشغال‌بودن پورت ۳۰۰۰ توسط پروژه‌ای دیگر، نسخه فعلی Personal Agent روی `http://localhost:3001` اجرا می‌شود.

```mermaid
flowchart LR
    A[پایه فنی و امنیت<br/>100٪] --> B[کارها و جلسات<br/>100٪]
    B --> C[تقویم واقعی<br/>75٪]
    C --> D[شناخت و ترجیحات کاربر<br/>100٪]
    D --> E[مرکز اعلان و یادآوری<br/>85٪]
    E --> F[اتصال کامل به LLM<br/>75٪]
    F --> G[آزمون نهایی و اپ PWA<br/>60٪]
    G --> H[انتقال دامنه<br/>منتظر تأیید نهایی]

    classDef done fill:#e4f0e5,stroke:#8ca088,color:#26312c;
    classDef active fill:#f8eee8,stroke:#d7a98f,color:#26312c;
    classDef pending fill:#efedf7,stroke:#aa9dbc,color:#26312c;
    class A,B,D done;
    class C,E,F active;
    class G,H pending;
```

## معنی هر بخش

| بخش | وضعیت واقعی | کار باقی‌مانده |
|---|---:|---|
| پایه فنی، ورود، دیتابیس و امنیت | ۱۰۰٪ | فقط نگهداری و بررسی دوره‌ای |
| کارها و جلسات | ۱۰۰٪ | ایجاد، ویرایش، حذف دومرحله‌ای و هماهنگی یادآوری‌ها کامل است |
| تقویم | ۷۵٪ | جابه‌جایی بازه‌ها و ویرایش مستقیم |
| شناخت کاربر | ۱۰۰٪ | ساعت و روز کاری، زمان سکوت، سبک برنامه‌ریزی و یادآوری پیش‌فرض ذخیره می‌شود |
| اعلان‌ها | ۸۵٪ | مرکز اعلان کامل است؛ ارسال Push نهایی به کلید VAPID و HTTPS دامنه وابسته است |
| LLM و Agent | ۷۵٪ | زمینه کارها، جلسات و ترجیحات و حافظه گفتگو آماده است؛ کلید سرویس و آزمایش زنده باقی مانده |
| اپ نصب‌شونده و کنترل کیفیت | ۶۰٪ | آزمایش موبایل، نصب و سناریوهای نهایی |
| سرور و دامنه | آماده‌سازی انجام شده | دامنه فقط پس از پایان پروژه و تأیید مالک منتقل می‌شود |

## قانون به‌روزرسانی این نقشه

پس از هر قابلیت کامل، این فایل باید همراه با نتیجه تست‌ها، مرحله فعلی و قدم بعدی به‌روزرسانی و در GitHub ثبت شود.

## آخرین کنترل کیفیت

- Type Check: موفق
- Lint: موفق
- تست خودکار: ۱۳ تست موفق
- Build نهایی: موفق
- سلامت API و اتصال دیتابیس: موفق
- آزمایش واقعی مرورگر: رابط کامل روی `localhost:3001` بدون خطای Console اجرا شد و مدیریت امن نبود کلید سرویس موفق است

## اقدام بیرونی موردنیاز

- محیط محلی و فایل محرمانه `.env` آماده است، اما `OPENAI_API_KEY` هنوز تنظیم نشده است. این کلید باید مستقیماً در همان فایل قرار بگیرد و هرگز وارد Git یا پیام عمومی نشود.
- کلیدهای VAPID برای Push نیز هنوز تنظیم نشده‌اند؛ آزمایش نهایی Push پس از آماده‌شدن HTTPS دامنه انجام می‌شود.
