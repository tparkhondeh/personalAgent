# همراه — Personal Agent

همراه یک دستیار شخصی فارسی و مینیمال برای مدیریت کارهای شخصی، کارهای شرکتی، جلسات، تقویم و یادآوری‌هاست. دستیار فعلاً در حالت محلی کار می‌کند؛ اتصال واقعی LLM آماده فعال‌سازی است، اما کلید سرویس هنوز تنظیم نشده است. پیشنهادهای دارای اثر فقط پس از تأیید کاربر اجرا می‌شوند.

## آخرین نسخه قابل‌آزمایش — ۲۰۲۶-۰۹-۰۶ UTC

- [لوکال این دستگاه](http://localhost:3001) و [Staging پایدار](https://personalagent.wealthos.ir:8443)
- [دانلود همراه پایدار ۳۴](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-stable-34/Hamrah-stable-34.apk)
- [مقایسه کد، Staging و APK؛ شناسه فایل، شواهد و محدودیت‌های آزمون](docs/CLEAN_INTERFACE_2026-09-06.md)

APK و Staging از `8db5cf4eb44207efd0d8ebecbae089e4e10143f9` هستند. همان فایل بدون بازسازی روی Android 13/14/16 در اجرای `34052215051` موفق شد؛ ۹ تصویر روشن/بازیابی تیره/کیبورد بدون پوشش خطای سیستم بصری بررسی شدند. فایل Release دوباره دانلود و SHA آن با فایل آزموده‌شده برابر شد. Package برابر `ir.wealthos.personalagent.stable34` و SHA برابر `b605b522e253e75181e668040f995bc3c1e739a15c224da2ae1300f5935339eb` است. Production تغییر نکرده؛ برنامه قبلی گوشی را حذف نکنید، داده محلی بین Packageهای جدا خودکار منتقل نمی‌شود. لینک‌های تاریخی پایین، نسخه پیشنهادی نصب نیستند.

نسخه ۳۴ آیکون‌های تنظیمات/اعلان/+، حذف متن‌های اضافی، اسکرول داخلی امروز/کارها، کادر یک‌خطی قابل رشد، تایپ مهمان و حفظ پیش‌نویس هنگام ورود و «مرا به خاطر بسپار» دارد. پالت روشن پیش‌فرض، تم تیره صریح، شعر دو ستونه، شمسی و ۲۴ساعته حفظ شدند. ۲۳۵ تست محلی، پنج تست واحد Android، ۱۱ تست بومی و پنج مجموعه ضبط/لغو/تطبیق/کیبورد در هر Android موفق‌اند؛ ۴۶ کنترل HTTP روی لوکال و ۴۶ روی Staging نیز موفق شد. ضبط و پخش صوت محلی مستقل کار می‌کند؛ تبدیل صوت واقعی، LLM، Push واقعی و تماس هنوز فعال نیستند. میکروفون فیزیکی مرورگر میزبان موجود نبود و مسیر خطا آزموده شد. runner به Staging دسترسی نداشت و recovery آزموده شد؛ وب آنلاین با مرورگر/HTTP جدا بررسی شده، نه روی شبکه و میکروفون گوشی مالک. پذیرش همین نسخه روی گوشی باقی است.

## امکانات MVP

- رابط RTL، Responsive، پاستلی و Dark Mode با فونت محلی وزیرمتن
- شعر روز مولانا با ۳۶۰ رباعی کامل و معتبر، ثابت برای هر روز و قابل استفاده بدون اینترنت
- کارهای شخصی و شرکتی با اولویت فوری، مهم و عادی
- جلسات و نمای تقویم هفتگی
- تنظیمات کاربر شامل ساعت کاری، روزهای کاری و زمان‌های یادآوری؛ گزینه سبک برنامه‌ریزی حذف شده است
- ثبت‌نام و ورود امن با Better Auth
- SQLite/LibSQL و Prisma با جداسازی داده هر کاربر
- Audit Log برای عملیات داده
- گفتگوی ساختاریافته با حالت محلی یا LLM و تأیید پیش از اجرا
- دو یا سه یادآوری پیش‌فرض هم‌زمان برای هر کار و جلسه، مرکز اعلان، اعلان محلی و زیرساخت Web Push
- PWA قابل نصب روی موبایل و دسکتاپ با پوسته آفلاین پایه
- پروژه بومی Android با Capacitor، آیکون و Splash اختصاصی
- ساخت APK، آزمایش خودکار Android 13/14/16 و پیش‌نمایش HTTPS پایدار قابل‌نصب بدون کابل در GitHub Actions
- هشدار چندمرحله‌ای کار فوری: اعلان، Alarm اندروید، اولویت بالا، Mock امن پیامک و تماس واقعیِ محافظت‌شده و اختیاری
- Backup سازگار SQLite، کنترل Rollback و هدرهای امنیتی
- تست Unit، Type Check، Lint، Build و CI

## اجرای محلی

پیش‌نیاز: Node.js 24 و pnpm 11.

```powershell
pnpm install
Copy-Item .env.example .env
pnpm db:generate
pnpm db:init
pnpm dev
```

سپس `http://localhost:3000` را باز کنید. مقادیر Secret و کلید API فقط در `.env` قرار می‌گیرند؛ این فایل وارد Git نمی‌شود.

## کنترل کیفیت

```bash
pnpm typecheck
pnpm lint
pnpm test
pnpm build
pnpm audit --prod --audit-level high
```

## اپلیکیشن Android

نسخه آزمایشی فعلی «همراه پایدار 28» با شناسه `ir.wealthos.personalagent.stable28` است. همین فایل روی Android 13، 14 و 16 آزمون شده؛ محدودیت اتصال آنلاین runner و شواهد تصویری در گزارش بالا ثبت است و پذیرش گوشی مالک هنوز معوق است. نسخه قدیمی را برای حفظ داده محلی حذف نکنید؛ اطلاعات محلی بین شناسه‌های مستقل خودکار منتقل نمی‌شوند. برای جلوگیری از تکرار مشکل صفحه سفید، علت‌ها، راه‌حل‌ها و کنترل‌های اجباری در [راهنمای دائمی ساخت و تحویل Android](docs/ANDROID_RELEASE_PLAYBOOK.md) ثبت شده‌اند.

نسخه آزمایشی Android ساخته شده و با `pnpm android:build:debug` دوباره تولید می‌شود. خروجی محلی در `android/app/build/outputs/apk/debug/app-debug.apk` قرار می‌گیرد. پس از اتصال و تأیید USB debugging روی یک گوشی، دستور `pnpm android:install:device` ساخت، اتصال امن محلی، نصب و بازکردن برنامه را خودکار انجام می‌دهد. نسخه عمومیِ امضاشده تا تأیید آدرس امن Production ساخته نمی‌شود.

### یکسان‌بودن ظاهر اپ با نسخه لوکال

نسخه‌های نصب‌شده خودکار با Commit شدن کد تازه نمی‌شوند: رابط متصل از Release فعال Staging خوانده می‌شود و رابط آفلاین از فایل‌های داخل همان APK. پس از تغییرات وب، هر دو باید جداگانه بررسی و به‌روزرسانی شوند. Workflow پایدار اکنون APK و بسته Linux همان Commit را با هم می‌سازد. شعر و تاریخ و سه یادآوری در رابط محلی نیز هماهنگ شده‌اند؛ داده‌های حالت محلی با حساب سرور همگام نمی‌شوند.

در APK پایدار فعلی **دو حالت** وجود دارد:

- **متصل به سرور (`server.url`)**: همان نسخه‌ای است که باید UI آن دقیقاً شبیه مرورگر لوکال شود؛ مناسب برای تأیید نهایی.
- **آفلاین (`mobile-shell`)**: رابط سبک‌تر داخل همان APK برای سناریوی بدون اینترنت؛ فونت، شعر، تاریخ و سه یادآوری هماهنگ‌اند، ولی چیدمان و داده مستقل هستند.

برای خروجی متصل، به‌صورت پیش‌فرض در محیط توسعه با `pnpm android:build:debug` ساخته می‌شود و باید روی `localhost:3001` (یا استیجینگ معادل) تست شود.

مسیر معتبر فعلی تحویل، Workflow `Verified stable Android preview` است: [اجرای موفق نسخه ۲۲](https://github.com/tparkhondeh/personalAgent/actions/runs/34013442504). APK فقط پس از موفقیت هر سه Android منتشر می‌شود؛ همان فایل تحویلی نصب و آزموده شده است. روش‌های موقت زیر فقط سابقه‌اند و برای تحویل تازه نباید استفاده شوند.

<details>
<summary>سوابق نسخه‌های قدیمی؛ برای نصب تازه استفاده نشوند</summary>

Workflow مستقل `Android emulator QA` برنامه را بدون گوشی روی Android 16 ابری Build، نصب و اجرا می‌کرد و نمایش اعلان، اجرای Alarm دقیق و لغو Alarm را نیز می‌آزمود. [اجرای تاریخی Android 16](https://github.com/tparkhondeh/personalAgent/actions/runs/33521413925) برای Commit پالت پاستیلی در GitHub Actions ثبت شده است.

برای نصب آزمایشی بدون کابل، Workflow دستی `Temporary phone preview` یک محیط کاملاً جدا با HTTPS و دیتابیس موقت می‌سازد، APK را به همان آدرس متصل می‌کند و لینک دانلود را در Summary اجرا قرار می‌دهد. [اجرای پیش‌نمایش شماره ۱](https://github.com/tparkhondeh/personalAgent/actions/runs/33493716896) بدون تغییر Production ساخته و کنترل شده است. لینک آن حدود دو ساعت فعال است؛ داده‌های این محیط موقت‌اند و نباید برای اطلاعات مهم استفاده شود.

برای آزمایش پایدار بدون دامنه، [همراه آفلاین ۳](https://github.com/tparkhondeh/personalAgent/releases/download/phone-preview-offline-3/hamrah-offline-preview-3.apk) با شناسه مستقل `ir.wealthos.personalagent.offlinepreview3` منتشر شده است. این شناسه اجازه می‌دهد APK کنار نسخه‌های قبلی نصب شود و خطای تداخل Package ایجاد نکند؛ داده‌های آن فقط روی همان گوشی ذخیره می‌شوند.

کاندید نهایی پیش از Production در [اجرای شماره ۵](https://github.com/tparkhondeh/personalAgent/actions/runs/33521435867) ساخته و روی Staging تأیید شده است. این اجرا بسته مستقل سرور، Migration، Reminder، Escalation، وب و APK متصل به `https://personalagent.wealthos.ir` را در محیط تمیز آزمایش می‌کند. [APK آزمایشی RC5](https://github.com/tparkhondeh/personalAgent/releases/download/production-rc-5/hamrah-android-rc-5.apk) Debug-signed است؛ امضای خصوصی و انتشار عمومی فقط بعد از تأیید نهایی مالک انجام می‌شود.

</details>

## متغیرهای محیطی

نمونه کامل در `.env.example` است. Production حداقل به `DATABASE_URL`، تنظیمات Better Auth، کلیدهای VAPID و `CRON_SECRET` نیاز دارد. کلید LLM اختیاری است و نبود آن برنامه را به حالت محلی امن می‌برد.

در محیط توسعه، خالی‌بودن کلید LLM مانع کار برنامه نیست؛ دستیار به‌صورت خودکار از حالت محلی امن استفاده می‌کند. همچنین بدون کلید VAPID می‌توان اعلان محلی همین دستگاه را آزمایش کرد.

### فعال‌سازی سرویس‌های بیرونی

- دستیار بدون کلید در حالت محلی کار می‌کند. برای اتصال واقعی OpenAI باید `OPENAI_API_KEY` در فایل محیطی امن سرور قرار گیرد و سپس یک درخواست آزمایشی کنترل‌شده اجرا شود.
- تماس واقعی برای شماره‌های ایران از مسیر کاوه‌نگار آماده شده است. فعال‌شدن آن هم‌زمان به `OUTBOUND_CALLS_MODE=live`، کلید `KAVENEGAR_API_KEY`، قرارگرفتن شماره تأییدشده در `OUTBOUND_CALL_VERIFIED_NUMBERS` و روشن‌بودن گزینه تماس در حساب کاربر نیاز دارد.
- تا وقتی هر چهار شرط تماس برقرار نباشد، تماس فقط شبیه‌سازی و در Audit Log ثبت می‌شود. متن تماس عمومی است و عنوان یا جزئیات خصوصی کار به سرویس تماس فرستاده نمی‌شود.

## مستندات

- [مختصات و درصد پیشرفت پروژه](PROJECT_STATUS.md)
- [پرامپت آماده برای ادامه پروژه](docs/continuation-prompt-fa.md)
- [معماری سیستم](docs/architecture.md)
- [منابع متن‌باز و مجوزها](docs/open-source-review.md)
- [برنامه Deploy، Backup و Rollback](docs/deployment.md)
- [راهنمای اپ Android](docs/android.md)
- [راهنمای جلوگیری از تکرار خطاهای Android](docs/ANDROID_RELEASE_PLAYBOOK.md)
- [منبع و روش انتخاب شعر روز مولانا](docs/rumi-daily.md)
- [کنترل‌های امنیت، Backup و Rollback](docs/security-backup-rollback.md)

مجوز انتشار اختصاصی پروژه هنوز توسط مالک تعیین نشده است. کپی مستقیم از پروژه‌های AGPL انجام نشده و فقط از الگوهای معماری عمومی آن‌ها استفاده شده است.
